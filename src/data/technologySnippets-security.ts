import { TechSnippetData } from "./technologySnippets";

const s = (c: string) => c.split("\n");

export const crowdstrike: TechSnippetData = {
  snippets: [
    { title: "falcon_api.py", language: "Python", badge: "🔒 Secure Code", badgeColor: "#0059B3", description: "Falcon API with OAuth2 authentication. At WebQ, we integrate CrowdStrike APIs with secure credential management.", lines: s("from falconpy import Hosts, OAuth2\n\nauth = OAuth2(client_id=os.environ['CS_CLIENT_ID'],\n              client_secret=os.environ['CS_CLIENT_SECRET'])\n\nhosts = Hosts(auth_object=auth)\n\n# Query endpoints with filters\nresponse = hosts.query_devices_by_filter(\n    filter=\"platform_name:'Windows'+last_seen:>'2024-01-01'\",\n    sort='last_seen.desc',\n    limit=100\n)\n\nif response['status_code'] == 200:\n    device_ids = response['body']['resources']\n    details = hosts.get_device_details(ids=device_ids)") },
    { title: "rtr_response.py", language: "Python", badge: "⚡ Performance", badgeColor: "#0070C0", description: "Real-Time Response for rapid containment. At WebQ, we automate incident response with CrowdStrike RTR scripts.", lines: s("from falconpy import RealTimeResponse\n\nrtr = RealTimeResponse(auth_object=auth)\n\n# Initiate RTR session\nsession = rtr.init_session(device_id=device_id)\nsession_id = session['body']['resources'][0]['session_id']\n\n# Execute commands\nrtr.execute_command(\n    session_id=session_id,\n    base_command='runscript',\n    command_string='runscript -CloudFile=\"CollectArtifacts\"'\n)\n\n# Network containment\nfrom falconpy import Hosts\nhosts = Hosts(auth_object=auth)\nhosts.perform_action(\n    action_name='contain',\n    ids=[device_id]\n)") },
    { title: "detection_test.py", language: "Python", badge: "🧪 Testing", badgeColor: "#0080CC", description: "Detection testing with MITRE ATT&CK mapping. At WebQ, we validate CrowdStrike detections against ATT&CK techniques.", lines: s("import pytest\nfrom falconpy import Detections\n\ndef test_detection_coverage():\n    detects = Detections(auth_object=auth)\n    response = detects.query_detects(\n        filter=\"created_timestamp:>'2024-01-01'\"\n    )\n    assert response['status_code'] == 200\n\ndef test_critical_severity_response():\n    response = detects.query_detects(\n        filter=\"max_severity_displayname:'Critical'\"\n    )\n    detections = response['body']['resources']\n    for d in detections:\n        detail = detects.get_detect_summaries(ids=[d])\n        status = detail['body']['resources'][0]['status']\n        assert status != 'new', f'Unhandled critical: {d}'") },
    { title: "siem_integration.py", language: "Python", badge: "📝 Logging", badgeColor: "#0090D6", description: "Event streaming to SIEM. At WebQ, we forward CrowdStrike events to our SIEM for unified security monitoring.", lines: s("from falconpy import EventStreams\n\nstreams = EventStreams(auth_object=auth)\n\n# List available streams\nstream_list = streams.list_available_streams(app_id='webq-siem')\ndata_feed_url = stream_list['body']['resources'][0]['dataFeedURL']\ntoken = stream_list['body']['resources'][0]['sessionToken']\n\n# Stream events to SIEM\nimport requests\nwith requests.get(data_feed_url,\n    headers={'Authorization': f'Token {token}'},\n    stream=True\n) as response:\n    for line in response.iter_lines():\n        if line:\n            forward_to_siem(json.loads(line))") },
    { title: "incident_handler.py", language: "Python", badge: "🛡️ Exception Handling", badgeColor: "#007AB8", description: "Automated incident response workflow. At WebQ, we automate containment and escalation for high-severity incidents.", lines: s("from falconpy import Incidents\n\ndef handle_incident(incident_id):\n    incidents = Incidents(auth_object=auth)\n    try:\n        detail = incidents.get_incident_details(ids=[incident_id])\n        incident = detail['body']['resources'][0]\n        severity = incident['fine_score']\n\n        if severity >= 80:  # Critical\n            # Auto-contain affected hosts\n            for host in incident['hosts']:\n                contain_host(host['device_id'])\n            escalate_to_soc(incident)\n        elif severity >= 50:  # High\n            create_ticket(incident)\n\n    except Exception as e:\n        log_error(f'Incident handling failed: {e}')\n        escalate_to_soc_manual(incident_id)") },
    { title: "dashboard_query.py", language: "Python", badge: "👤 User-Friendly", badgeColor: "#0099CC", description: "Security posture dashboard data. At WebQ, we build executive dashboards from CrowdStrike telemetry.", lines: s("from falconpy import Hosts, Detections\n\ndef get_security_posture():\n    hosts = Hosts(auth_object=auth)\n    detects = Detections(auth_object=auth)\n\n    # Endpoint health\n    total = hosts.query_devices_by_filter(limit=1)\n    online = hosts.query_devices_by_filter(\n        filter=\"status:'normal'\", limit=1\n    )\n\n    # Detection summary\n    critical = detects.query_detects(\n        filter=\"max_severity_displayname:'Critical'+status:'new'\"\n    )\n\n    return {\n        'total_endpoints': total['body']['meta']['pagination']['total'],\n        'online_pct': online['body']['meta']['pagination']['total'] / total,\n        'open_critical': len(critical['body']['resources']),\n    }") },
    { title: "ioc_management.py", language: "Python", badge: "📐 Best Practice", badgeColor: "#1A6BAF", description: "IOC management with custom indicators. At WebQ, we maintain curated IOC feeds integrated with CrowdStrike.", lines: s("from falconpy import IOC\n\nioc = IOC(auth_object=auth)\n\n# Upload custom IOC\nioc.indicator_create(\n    body={\n        'indicators': [{\n            'type': 'domain',\n            'value': 'malicious-site.com',\n            'action': 'detect',\n            'severity': 'high',\n            'description': 'Phishing domain',\n            'tags': ['phishing', 'webq-threat-intel'],\n            'platforms': ['windows', 'mac', 'linux'],\n            'expiration': '2024-12-31T00:00:00Z'\n        }]\n    }\n)") },
    { title: "threat_hunting.py", language: "Python", badge: "🔄 Event-Driven", badgeColor: "#0085C2", description: "Threat hunting with event search. At WebQ, we run proactive threat hunts using CrowdStrike's event search API.", lines: s("from falconpy import EventSearch\n\nsearch = EventSearch(auth_object=auth)\n\n# Hunt for suspicious PowerShell\nresult = search.search_events(\n    body={\n        'filter': 'event_type:ProcessRollup2',\n        'query': 'FileName:powershell.exe AND '\n                 'CommandLine:*-EncodedCommand*',\n        'time_range': {'from': 'now-7d', 'to': 'now'},\n        'limit': 100\n    }\n)\n\nfor event in result['body']['resources']:\n    if is_suspicious(event):\n        create_detection(event)\n        log_hunt_finding(event)") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#3378B5", description: "CrowdStrike integration runbook. At WebQ, we document every security integration with response procedures.", lines: s("# CrowdStrike Falcon Integration\n\n## Capabilities\n- EDR: Endpoint detection & response\n- RTR: Real-time remote response\n- Threat Intel: IOC management\n- Event Streaming: SIEM integration\n\n## Response SLAs\n| Severity | Response | Containment |\n|----------|----------|-------------|\n| Critical | 15 min | Auto |\n| High | 1 hour | 4 hours |\n| Medium | 4 hours | 24 hours |\n\n## Contacts\n- SOC: soc@webq.com\n- Escalation: security-oncall") },
  ],
  metrics: [
    { value: "#1", label: "EDR Platform", detail: "Gartner Leader" },
    { value: "23K+", label: "Customers", detail: "Global enterprises" },
    { value: "$3B+", label: "Annual Revenue", detail: "Market leader" },
    { value: "AI-Powered", label: "Detection", detail: "ML threat models" },
    { value: "1 Sec", label: "Detection Time", detail: "Real-time alerts" },
    { value: "Cloud-Native", label: "Architecture", detail: "No on-prem infra" },
    { value: "200+", label: "Adversaries Tracked", detail: "Threat intelligence" },
    { value: "MITRE", label: "ATT&CK Leader", detail: "Top evaluation scores" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 85, use: "Endpoint protection, compliance" },
    { industry: "Healthcare", adoption: 78, use: "HIPAA endpoint security" },
    { industry: "Government", adoption: 80, use: "FedRAMP endpoint protection" },
    { industry: "Technology", adoption: 82, use: "Developer workstation security" },
    { industry: "Retail", adoption: 70, use: "POS and endpoint protection" },
    { industry: "Energy", adoption: 68, use: "OT/IT endpoint security" },
  ],
  webqCallout: "At WebQ, we deploy CrowdStrike Falcon with automated incident response, SIEM integration, and proactive threat hunting — delivering enterprise-grade endpoint security with rapid containment.",
};

export const splunk: TechSnippetData = {
  snippets: [
    { title: "rbac_config.conf", language: "INI", badge: "🔒 Secure Code", badgeColor: "#0059B3", description: "Role-based access with index restrictions. At WebQ, we enforce least-privilege Splunk access with custom roles.", lines: s("[role_soc_analyst]\nimportRoles = user\nsrchIndexesAllowed = security;firewall;endpoint\nsrchIndexesDefault = security\nsrchFilter = source!=*sensitive*\nsrchTimeWin = 90\nctz = UTC\n\n[role_soc_lead]\nimportRoles = soc_analyst\nsrchIndexesAllowed = security;firewall;endpoint;audit\ncapabilities = schedule_search;accelerate_search\nsrchTimeWin = -1\n\n[role_compliance]\nimportRoles = user\nsrchIndexesAllowed = audit;compliance\nsrchFilter = sourcetype=audit_trail") },
    { title: "optimized_search.spl", language: "SPL", badge: "⚡ Performance", badgeColor: "#0070C0", description: "Optimized SPL with tstats and data models. At WebQ, we write efficient searches with accelerated data models.", lines: s("| tstats count as event_count\n    max(_time) as last_seen\n    values(Authentication.src) as sources\n  FROM datamodel=Authentication\n  WHERE Authentication.action=failure\n    Authentication.app=vpn\n  BY Authentication.user\n  | rename Authentication.* as *\n  | where event_count > 10\n  | sort -event_count\n  | lookup user_info user OUTPUT department\n  | table user, event_count, last_seen,\n          sources, department\n\n`comment(\"Uses tstats for 100x faster\n  than raw search. Data model acceleration\n  pre-indexes common fields.\")") },
    { title: "test_alert.py", language: "Python", badge: "🧪 Testing", badgeColor: "#0080CC", description: "Splunk search testing with SDK. At WebQ, we validate correlation rules with automated test cases.", lines: s("import splunklib.client as client\nimport splunklib.results as results\n\ndef test_brute_force_detection():\n    service = client.connect(\n        host='splunk.webq.com', port=8089,\n        username='test', password='test'\n    )\n    job = service.jobs.create(\n        'search index=security sourcetype=auth '\n        'action=failure | stats count by user '\n        '| where count > 10',\n        earliest_time='-24h'\n    )\n    while not job.is_done():\n        time.sleep(1)\n\n    reader = results.JSONResultsReader(job.results())\n    alerts = list(reader)\n    assert len(alerts) >= 0, 'Search executed successfully'") },
    { title: "audit_logging.spl", language: "SPL", badge: "📝 Logging", badgeColor: "#0090D6", description: "Audit trail with search activity monitoring. At WebQ, we track all Splunk user activity for compliance.", lines: s("index=_audit action=search\n  info=granted OR info=completed\n| stats count as search_count\n    values(search) as searches\n    max(_time) as last_search\n  BY user\n| sort -search_count\n| head 20\n\n| append\n  [search index=_internal sourcetype=splunkd_ui_access\n   | stats count as page_views BY user\n   | sort -page_views]\n\n| table user, search_count, last_search, page_views") },
    { title: "error_correlation.spl", language: "SPL", badge: "🛡️ Exception Handling", badgeColor: "#007AB8", description: "Multi-stage correlation for incident detection. At WebQ, we build correlation searches with risk scoring.", lines: s("| tstats summariesonly=true count\n    FROM datamodel=Authentication\n    WHERE Authentication.action=failure\n  BY Authentication.user, _time span=5m\n| where count > 5\n| join type=inner user\n  [search index=vpn action=connect\n   | stats values(src_ip) as vpn_ips BY user]\n| join type=left user\n  [search index=endpoint\n   sourcetype=process_creation\n   process_name=powershell.exe\n   | stats count as ps_count BY user]\n| eval risk_score = count*10 +\n    if(isnotnull(ps_count), ps_count*20, 0)\n| where risk_score > 50\n| sort -risk_score") },
    { title: "dashboard.xml", language: "XML", badge: "👤 User-Friendly", badgeColor: "#0099CC", description: "Interactive security dashboard. At WebQ, we build role-based Splunk dashboards with drilldown and tokens.", lines: s("<dashboard version=\"1.1\">\n  <label>Security Operations Center</label>\n  <row>\n    <panel>\n      <title>Critical Alerts (Last 24h)</title>\n      <single>\n        <search>\n          <query>\n            index=security severity=critical\n            | stats count\n          </query>\n          <earliest>-24h</earliest>\n        </search>\n        <drilldown>\n          <link target=\"_blank\">\n            /app/security/alert_detail?severity=critical\n          </link>\n        </drilldown>\n      </single>\n    </panel>\n  </row>\n</dashboard>") },
    { title: "deployment.conf", language: "INI", badge: "📐 Best Practice", badgeColor: "#1A6BAF", description: "Distributed deployment architecture. At WebQ, we design Splunk architectures with indexer clustering and search head pools.", lines: s("[indexer_cluster]\nmaster_uri = https://cm.webq.com:8089\nreplication_factor = 3\nsearch_factor = 2\n\n[search_head_cluster]\ndeployer_uri = https://deployer.webq.com:8089\n\n# inputs.conf for universal forwarder\n[monitor:///var/log/syslog]\nindex = os\nsourcetype = syslog\n\n[monitor:///var/log/auth.log]\nindex = security\nsourcetype = linux_secure\n\n# outputs.conf\n[tcpout:indexers]\nserver = idx1.webq.com:9997,idx2.webq.com:9997\nautoLBFrequency = 30") },
    { title: "adaptive_response.py", language: "Python", badge: "🔄 Event-Driven", badgeColor: "#0085C2", description: "Adaptive response actions for SOAR. At WebQ, we automate response with Splunk SOAR playbooks.", lines: s("# Splunk SOAR Playbook\nimport phantom.rules as phantom\n\ndef on_start(container):\n    # Get alert details\n    success, artifacts = phantom.get_artifacts(\n        container_id=container['id'])\n\n    for artifact in artifacts:\n        ip = artifact['cef'].get('sourceAddress')\n        if ip:\n            # Enrich with threat intel\n            phantom.act('ip reputation',\n                parameters=[{'ip': ip}],\n                assets=['virustotal'],\n                callback=process_results)\n\ndef process_results(action_results):\n    for result in action_results:\n        if result.get_data()[0]['malicious']:\n            phantom.act('block ip',\n                parameters=[{'ip': result['ip']}],\n                assets=['firewall'])") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#3378B5", description: "SIEM architecture documentation. At WebQ, we document Splunk deployments with data onboarding and retention policies.", lines: s("# Splunk SIEM Platform\n\n## Architecture\n- 3x Indexers (clustered, RF=3)\n- 2x Search Heads (SHC)\n- 1x Cluster Manager\n- Universal Forwarders on all hosts\n\n## Data Sources\n| Source | Index | Retention |\n|--------|-------|----------|\n| Firewall | security | 90 days |\n| Endpoint | endpoint | 30 days |\n| Auth logs | security | 1 year |\n\n## Alerts\n- Brute force: >10 failures/5min\n- Malware: CrowdStrike detection\n- Data exfil: >1GB upload") },
  ],
  metrics: [
    { value: "#1", label: "SIEM Platform", detail: "Market leader" },
    { value: "Cisco", label: "Owned By", detail: "$28B acquisition" },
    { value: "15K+", label: "Customers", detail: "Enterprise deployments" },
    { value: "SPL", label: "Query Language", detail: "Powerful search" },
    { value: "PB-Scale", label: "Data Ingestion", detail: "Daily volume" },
    { value: "2,400+", label: "Apps", detail: "Splunkbase ecosystem" },
    { value: "SOAR", label: "Built-in", detail: "Automated response" },
    { value: "1 Min", label: "Time to Alert", detail: "Real-time detection" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 85, use: "Fraud detection, compliance monitoring" },
    { industry: "Government", adoption: 82, use: "Security operations, FedRAMP" },
    { industry: "Healthcare", adoption: 75, use: "HIPAA monitoring, threat detection" },
    { industry: "Technology", adoption: 80, use: "Application monitoring, security" },
    { industry: "Retail", adoption: 65, use: "PCI compliance, fraud detection" },
    { industry: "Energy", adoption: 70, use: "OT security monitoring" },
  ],
  webqCallout: "At WebQ, we deploy Splunk with optimized data models, risk-based alerting, and SOAR automation — delivering enterprise security operations with rapid detection and response.",
};

export const paloAltoNetworks: TechSnippetData = {
  snippets: [
    { title: "security_policy.xml", language: "XML", badge: "🔒 Secure Code", badgeColor: "#0059B3", description: "Zero-trust security policies. At WebQ, we implement micro-segmentation with app-aware firewall rules.", lines: s("<security-rule>\n  <name>Allow-Web-to-API</name>\n  <from>web-zone</from>\n  <to>api-zone</to>\n  <source>web-servers</source>\n  <destination>api-servers</destination>\n  <application>ssl, web-browsing</application>\n  <service>application-default</service>\n  <action>allow</action>\n  <log-start>yes</log-start>\n  <log-end>yes</log-end>\n  <profile-setting>\n    <profiles>\n      <virus>strict</virus>\n      <spyware>strict</spyware>\n      <vulnerability>strict</vulnerability>\n    </profiles>\n  </profile-setting>\n</security-rule>") },
    { title: "threat_prevention.xml", language: "XML", badge: "⚡ Performance", badgeColor: "#0070C0", description: "Optimized threat prevention profiles. At WebQ, we tune IPS signatures for minimal latency with maximum detection.", lines: s("<anti-spyware>\n  <name>webq-strict</name>\n  <rules>\n    <rule>\n      <name>Block-Critical-High</name>\n      <severity>critical,high</severity>\n      <action>reset-both</action>\n      <packet-capture>single-packet</packet-capture>\n    </rule>\n    <rule>\n      <name>Alert-Medium</name>\n      <severity>medium</severity>\n      <action>alert</action>\n    </rule>\n  </rules>\n  <botnet-domains>\n    <lists>\n      <default-paloalto-dns>block</default-paloalto-dns>\n    </lists>\n    <dns-security-categories>\n      <malware>block</malware>\n      <phishing>block</phishing>\n    </dns-security-categories>\n  </botnet-domains>\n</anti-spyware>") },
    { title: "policy_test.py", language: "Python", badge: "🧪 Testing", badgeColor: "#0080CC", description: "Firewall policy testing with pan-os-python. At WebQ, we validate security policies with automated compliance tests.", lines: s("from panos.firewall import Firewall\nfrom panos.policies import SecurityRule\n\ndef test_default_deny():\n    fw = Firewall(hostname, api_key=api_key)\n    rules = SecurityRule.refreshall(fw)\n    last_rule = rules[-1]\n    assert last_rule.action == 'deny', \\\n        'Last rule must be default deny'\n\ndef test_no_any_any_allow():\n    fw = Firewall(hostname, api_key=api_key)\n    rules = SecurityRule.refreshall(fw)\n    for rule in rules:\n        if rule.action == 'allow':\n            assert rule.source != ['any'] or \\\n                   rule.destination != ['any'], \\\n                f'Rule {rule.name} allows any-to-any'") },
    { title: "log_forwarding.xml", language: "XML", badge: "📝 Logging", badgeColor: "#0090D6", description: "Log forwarding to SIEM. At WebQ, we forward all firewall logs to Splunk with CEF formatting.", lines: s("<log-forwarding-profile>\n  <name>siem-forwarding</name>\n  <match-list>\n    <entry name=\"threat-logs\">\n      <log-type>threat</log-type>\n      <send-to-panorama>yes</send-to-panorama>\n      <send-syslog>\n        <member>splunk-syslog</member>\n      </send-syslog>\n      <filter>All Logs</filter>\n    </entry>\n    <entry name=\"traffic-logs\">\n      <log-type>traffic</log-type>\n      <send-syslog>\n        <member>splunk-syslog</member>\n      </send-syslog>\n      <filter>Non-Internal Traffic</filter>\n    </entry>\n  </match-list>\n</log-forwarding-profile>") },
    { title: "incident_response.py", language: "Python", badge: "🛡️ Exception Handling", badgeColor: "#007AB8", description: "Automated blocking with Cortex XSOAR. At WebQ, we automate threat response with firewall API integration.", lines: s("from panos.firewall import Firewall\nfrom panos.objects import AddressObject\n\ndef block_malicious_ip(ip_address, reason):\n    fw = Firewall(hostname, api_key=api_key)\n    try:\n        addr = AddressObject(\n            name=f'blocked-{ip_address}',\n            value=ip_address,\n            description=f'Auto-blocked: {reason}'\n        )\n        fw.add(addr)\n        addr.create()\n\n        # Add to block group\n        # group.add_member(addr.name)\n        fw.commit(sync=True)\n        log_action(f'Blocked {ip_address}: {reason}')\n    except Exception as e:\n        log_error(f'Block failed for {ip_address}: {e}')\n        escalate_to_soc(ip_address, reason)") },
    { title: "prisma_cloud.py", language: "Python", badge: "👤 User-Friendly", badgeColor: "#0099CC", description: "Prisma Cloud for cloud security posture. At WebQ, we monitor cloud workloads with Prisma Cloud APIs.", lines: s("import requests\n\ndef get_cloud_posture():\n    token = authenticate_prisma()\n    headers = {'x-redlock-auth': token}\n\n    # Get compliance summary\n    response = requests.get(\n        f'{PRISMA_URL}/compliance/posture',\n        headers=headers,\n        params={'cloud.type': 'aws', 'timeType': 'relative'}\n    )\n\n    posture = response.json()\n    return {\n        'passed': posture['passedResources'],\n        'failed': posture['failedResources'],\n        'compliance_pct': posture['compliancePercentage'],\n        'critical_alerts': posture['criticalSeverityFailedResources']\n    }") },
    { title: "best_practice.xml", language: "XML", badge: "📐 Best Practice", badgeColor: "#1A6BAF", description: "BPA-compliant configuration. At WebQ, we follow Palo Alto Best Practice Assessment guidelines.", lines: s("<!-- Zone-based architecture -->\n<zone>\n  <entry name=\"trust\">\n    <network><layer3><member>ethernet1/1</member></layer3></network>\n  </entry>\n  <entry name=\"untrust\">\n    <network><layer3><member>ethernet1/2</member></layer3></network>\n  </entry>\n  <entry name=\"dmz\">\n    <network><layer3><member>ethernet1/3</member></layer3></network>\n  </entry>\n</zone>\n\n<!-- SSL decryption policy -->\n<ssl-decryption-rule>\n  <name>Decrypt-Outbound</name>\n  <from>trust</from>\n  <to>untrust</to>\n  <action>decrypt</action>\n</ssl-decryption-rule>") },
    { title: "cortex_xdr.py", language: "Python", badge: "🔄 Event-Driven", badgeColor: "#0085C2", description: "Cortex XDR for cross-platform detection. At WebQ, we correlate endpoint, network, and cloud alerts with Cortex XDR.", lines: s("import requests\n\ndef query_xdr_incidents():\n    response = requests.post(\n        f'{XDR_URL}/public_api/v1/incidents/get_incidents',\n        headers={'x-xdr-auth-id': AUTH_ID,\n                 'Authorization': API_KEY},\n        json={\n            'request_data': {\n                'filters': [{\n                    'field': 'severity',\n                    'operator': 'in',\n                    'value': ['high', 'critical']\n                }],\n                'sort': {\n                    'field': 'creation_time',\n                    'keyword': 'desc'\n                }\n            }\n        }\n    )\n    return response.json()['reply']['incidents']") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#3378B5", description: "Network security architecture docs. At WebQ, we document firewall policies, zones, and incident response procedures.", lines: s("# Network Security Architecture\n\n## Firewall Zones\n| Zone | Purpose | Interfaces |\n|------|---------|------------|\n| Trust | Internal | eth1/1-4 |\n| Untrust | Internet | eth1/5 |\n| DMZ | Public services | eth1/6 |\n\n## Security Profiles\n- Antivirus: Strict (block on detect)\n- Anti-Spyware: Strict (reset-both)\n- IPS: Critical+High (block)\n\n## Response SLAs\n- Critical threat: Auto-block + 15min SOC\n- High threat: Alert + 1hr investigation") },
  ],
  metrics: [
    { value: "#1", label: "Network Security", detail: "Gartner Leader" },
    { value: "80K+", label: "Customers", detail: "Global enterprises" },
    { value: "$6.9B", label: "Annual Revenue", detail: "Market leader" },
    { value: "Prisma", label: "Cloud Security", detail: "CSPM + CWPP" },
    { value: "Cortex", label: "XDR Platform", detail: "Cross-platform detection" },
    { value: "ML-Powered", label: "Detection", detail: "Inline ML models" },
    { value: "Zero Trust", label: "Architecture", detail: "ZTNA 2.0" },
    { value: "1,000+", label: "App Signatures", detail: "App-ID technology" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 85, use: "Network security, compliance" },
    { industry: "Government", adoption: 82, use: "FedRAMP, classified networks" },
    { industry: "Healthcare", adoption: 78, use: "HIPAA network security" },
    { industry: "Retail", adoption: 72, use: "PCI compliance, network segmentation" },
    { industry: "Technology", adoption: 80, use: "Cloud and network security" },
    { industry: "Energy", adoption: 68, use: "OT/IT convergence security" },
  ],
  webqCallout: "At WebQ, we design Palo Alto Networks security architectures with zero-trust policies, automated threat response, and Prisma Cloud governance — delivering comprehensive network and cloud security.",
};

export const hashicorpVault: TechSnippetData = {
  snippets: [
    { title: "vault_config.hcl", language: "HCL", badge: "🔒 Secure Code", badgeColor: "#0059B3", description: "Auto-unseal with KMS and audit logging. At WebQ, we configure Vault with cloud KMS auto-unseal and comprehensive audit trails.", lines: s("storage \"raft\" {\n  path    = \"/vault/data\"\n  node_id = \"vault-1\"\n}\n\nseal \"awskms\" {\n  region     = \"us-east-1\"\n  kms_key_id = \"alias/vault-unseal\"\n}\n\nlistener \"tcp\" {\n  address     = \"0.0.0.0:8200\"\n  tls_cert_file = \"/certs/vault.crt\"\n  tls_key_file  = \"/certs/vault.key\"\n}\n\naudit {\n  type = \"file\"\n  options {\n    file_path = \"/var/log/vault/audit.log\"\n    log_raw   = false\n  }\n}") },
    { title: "dynamic_secrets.hcl", language: "HCL", badge: "⚡ Performance", badgeColor: "#0070C0", description: "Dynamic database credentials with TTL. At WebQ, we generate short-lived database credentials for every application.", lines: s("# Enable database secrets engine\nvault secrets enable database\n\nvault write database/config/postgres \\\n  plugin_name=postgresql-database-plugin \\\n  connection_url=\"postgresql://{{username}}:{{password}}@db:5432/orders\" \\\n  allowed_roles=\"app-readonly,app-readwrite\" \\\n  username=\"vault_admin\" \\\n  password=\"admin_password\"\n\nvault write database/roles/app-readonly \\\n  db_name=postgres \\\n  creation_statements=\"CREATE ROLE \\\"{{name}}\\\" WITH LOGIN PASSWORD '{{password}}' VALID UNTIL '{{expiration}}'; GRANT SELECT ON ALL TABLES IN SCHEMA public TO \\\"{{name}}\\\";\" \\\n  default_ttl=\"1h\" \\\n  max_ttl=\"24h\"") },
    { title: "vault_test.sh", language: "Bash", badge: "🧪 Testing", badgeColor: "#0080CC", description: "Vault integration testing. At WebQ, we test secret retrieval and rotation in CI/CD pipelines.", lines: s("#!/bin/bash\nset -e\n\n# Test: Can authenticate with AppRole\nTOKEN=$(vault write -format=json auth/approle/login \\\n  role_id=$ROLE_ID secret_id=$SECRET_ID | jq -r '.auth.client_token')\nassert_not_empty \"$TOKEN\" \"Authentication failed\"\n\n# Test: Can read secret\nexport VAULT_TOKEN=$TOKEN\nSECRET=$(vault kv get -format=json secret/app/config | jq -r '.data.data.api_key')\nassert_not_empty \"$SECRET\" \"Secret retrieval failed\"\n\n# Test: Dynamic DB credential\nDB_CRED=$(vault read -format=json database/creds/app-readonly)\nDB_USER=$(echo $DB_CRED | jq -r '.data.username')\nassert_not_empty \"$DB_USER\" \"DB credential generation failed\"\n\necho 'All Vault tests passed ✅'") },
    { title: "audit_config.hcl", language: "HCL", badge: "📝 Logging", badgeColor: "#0090D6", description: "Multi-backend audit logging. At WebQ, we configure file and syslog audit backends for compliance.", lines: s("# File audit backend\nvault audit enable file \\\n  file_path=/var/log/vault/audit.log \\\n  log_raw=false \\\n  hmac_accessor=true\n\n# Syslog audit backend\nvault audit enable syslog \\\n  tag=\"vault\" \\\n  facility=\"AUTH\"\n\n# Audit log entry example:\n# {\n#   \"type\": \"response\",\n#   \"auth\": {\n#     \"token_type\": \"service\",\n#     \"policies\": [\"app-policy\"]\n#   },\n#   \"request\": {\n#     \"path\": \"secret/data/app/config\",\n#     \"operation\": \"read\"\n#   }\n# }") },
    { title: "error_handling.py", language: "Python", badge: "🛡️ Exception Handling", badgeColor: "#007AB8", description: "Vault client with retry and fallback. At WebQ, we implement resilient secret retrieval with caching and retry.", lines: s("import hvac\nfrom tenacity import retry, stop_after_attempt\n\n@retry(stop=stop_after_attempt(3))\ndef get_secret(path):\n    client = hvac.Client(url=VAULT_ADDR, token=VAULT_TOKEN)\n    try:\n        if not client.is_authenticated():\n            raise Exception('Vault auth failed')\n        response = client.secrets.kv.v2.read_secret_version(\n            path=path\n        )\n        return response['data']['data']\n    except hvac.exceptions.Forbidden:\n        logger.error(f'Access denied: {path}')\n        raise\n    except hvac.exceptions.VaultDown:\n        logger.error('Vault is sealed or unreachable')\n        return get_cached_secret(path)  # Fallback") },
    { title: "app_integration.py", language: "Python", badge: "👤 User-Friendly", badgeColor: "#0099CC", description: "Application integration with Agent sidecar. At WebQ, we use Vault Agent for transparent secret injection.", lines: s("# Vault Agent config (vault-agent.hcl)\nauto_auth {\n  method \"kubernetes\" {\n    mount_path = \"auth/kubernetes\"\n    config = {\n      role = \"order-service\"\n    }\n  }\n  sink \"file\" {\n    config = { path = \"/tmp/.vault-token\" }\n  }\n}\n\ntemplate {\n  source      = \"/vault/templates/config.ctmpl\"\n  destination = \"/app/config.json\"\n}\n\n# config.ctmpl\n{{ with secret \"secret/data/order-service\" }}\n{\n  \"db_url\": \"{{ .Data.data.db_url }}\",\n  \"api_key\": \"{{ .Data.data.api_key }}\"\n}\n{{ end }}") },
    { title: "policy.hcl", language: "HCL", badge: "📐 Best Practice", badgeColor: "#1A6BAF", description: "Least-privilege policies with path restrictions. At WebQ, we create granular Vault policies per service.", lines: s("# Policy: order-service\npath \"secret/data/order-service/*\" {\n  capabilities = [\"read\", \"list\"]\n}\n\npath \"database/creds/order-readonly\" {\n  capabilities = [\"read\"]\n}\n\npath \"transit/encrypt/order-key\" {\n  capabilities = [\"update\"]\n}\n\npath \"transit/decrypt/order-key\" {\n  capabilities = [\"update\"]\n}\n\n# Deny access to other services\npath \"secret/data/payment-service/*\" {\n  capabilities = [\"deny\"]\n}") },
    { title: "transit_encryption.py", language: "Python", badge: "🔄 Event-Driven", badgeColor: "#0085C2", description: "Transit secrets engine for encryption as a service. At WebQ, we use Vault Transit for application-level encryption.", lines: s("import hvac\nimport base64\n\nclient = hvac.Client(url=VAULT_ADDR, token=VAULT_TOKEN)\n\n# Encrypt sensitive data\ndef encrypt_pii(plaintext):\n    response = client.secrets.transit.encrypt_data(\n        name='pii-key',\n        plaintext=base64.b64encode(\n            plaintext.encode()\n        ).decode()\n    )\n    return response['data']['ciphertext']\n\n# Decrypt when needed\ndef decrypt_pii(ciphertext):\n    response = client.secrets.transit.decrypt_data(\n        name='pii-key',\n        ciphertext=ciphertext\n    )\n    return base64.b64decode(\n        response['data']['plaintext']\n    ).decode()") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#3378B5", description: "Vault operations runbook. At WebQ, we document Vault architecture, policies, and emergency procedures.", lines: s("# HashiCorp Vault Operations\n\n## Architecture\n- 3-node Raft cluster (HA)\n- AWS KMS auto-unseal\n- Consul service discovery\n\n## Auth Methods\n| Method | Use Case |\n|--------|----------|\n| AppRole | CI/CD, services |\n| Kubernetes | K8s pods |\n| OIDC | Human access |\n\n## Secrets Engines\n- KV v2: Static secrets\n- Database: Dynamic DB creds\n- Transit: Encryption as a service\n- PKI: Certificate management\n\n## Emergency\n1. Check seal status: vault status\n2. View audit: /var/log/vault/audit.log") },
  ],
  metrics: [
    { value: "#1", label: "Secrets Management", detail: "Industry standard" },
    { value: "HashiCorp", label: "Enterprise", detail: "IBM acquired" },
    { value: "30K+", label: "GitHub Stars", detail: "Open-source" },
    { value: "Dynamic", label: "Secrets", detail: "Auto-rotating credentials" },
    { value: "Transit", label: "Encryption", detail: "Encryption as a service" },
    { value: "Multi-Cloud", label: "Support", detail: "AWS, Azure, GCP" },
    { value: "100+", label: "Integrations", detail: "Auth & secrets backends" },
    { value: "FIPS", label: "Compliance", detail: "Enterprise certified" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 85, use: "Credential management, encryption" },
    { industry: "Healthcare", adoption: 75, use: "HIPAA secrets management" },
    { industry: "Technology", adoption: 88, use: "DevSecOps, dynamic credentials" },
    { industry: "Government", adoption: 70, use: "FedRAMP secrets management" },
    { industry: "Retail", adoption: 62, use: "PCI key management" },
    { industry: "Telecommunications", adoption: 65, use: "Infrastructure credentials" },
  ],
  webqCallout: "At WebQ, we implement HashiCorp Vault with dynamic credentials, transit encryption, and Kubernetes-native integration — delivering zero-trust secrets management at enterprise scale.",
};

export const sonarqube: TechSnippetData = {
  snippets: [
    { title: "sonar-project.properties", language: "Properties", badge: "🔒 Secure Code", badgeColor: "#0059B3", description: "Security hotspot detection configuration. At WebQ, we configure SonarQube for OWASP Top 10 vulnerability scanning.", lines: s("sonar.projectKey=webq-order-service\nsonar.projectName=Order Service\nsonar.sources=src/main\nsonar.tests=src/test\nsonar.java.binaries=target/classes\n\n# Security analysis\nsonar.security.hotspots.reviewed=true\nsonar.qualitygate.wait=true\n\n# Exclusions\nsonar.exclusions=**/generated/**,**/test/**\nsonar.coverage.exclusions=**/config/**,**/dto/**\n\n# Coverage\nsonar.coverage.jacoco.xmlReportPaths=\\\n  target/site/jacoco/jacoco.xml") },
    { title: "quality_gate.json", language: "JSON", badge: "⚡ Performance", badgeColor: "#0070C0", description: "Quality gates for automated enforcement. At WebQ, we enforce code quality thresholds before merge.", lines: s("{\n  \"name\": \"WebQ Production Gate\",\n  \"conditions\": [\n    {\n      \"metric\": \"new_reliability_rating\",\n      \"op\": \"GT\",\n      \"error\": \"1\"\n    },\n    {\n      \"metric\": \"new_security_rating\",\n      \"op\": \"GT\",\n      \"error\": \"1\"\n    },\n    {\n      \"metric\": \"new_coverage\",\n      \"op\": \"LT\",\n      \"error\": \"80\"\n    },\n    {\n      \"metric\": \"new_duplicated_lines_density\",\n      \"op\": \"GT\",\n      \"error\": \"3\"\n    },\n    {\n      \"metric\": \"new_security_hotspots_reviewed\",\n      \"op\": \"LT\",\n      \"error\": \"100\"\n    }\n  ]\n}") },
    { title: "pipeline_test.yml", language: "YAML", badge: "🧪 Testing", badgeColor: "#0080CC", description: "CI/CD integration with quality gate check. At WebQ, we fail builds that don't meet quality standards.", lines: s("# GitHub Actions SonarQube integration\nname: Code Quality\non: [pull_request]\njobs:\n  sonarqube:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n      - name: SonarQube Scan\n        uses: sonarsource/sonarqube-scan-action@master\n        env:\n          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}\n      - name: Quality Gate Check\n        uses: sonarsource/sonarqube-quality-gate-action@master\n        timeout-minutes: 5\n        env:\n          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}") },
    { title: "webhook_config.json", language: "JSON", badge: "📝 Logging", badgeColor: "#0090D6", description: "Webhook notifications for scan results. At WebQ, we send SonarQube results to Slack and project dashboards.", lines: s("{\n  \"name\": \"Slack Notification\",\n  \"url\": \"https://hooks.slack.com/services/...\",\n  \"events\": [\"PROJECT_ANALYSIS\"],\n  \"payload_template\": {\n    \"text\": \"SonarQube Analysis Complete\",\n    \"blocks\": [\n      {\n        \"type\": \"section\",\n        \"text\": {\n          \"type\": \"mrkdwn\",\n          \"text\": \"*Project:* {{project.name}}\\n*Status:* {{qualityGate.status}}\\n*Bugs:* {{bugs}}\\n*Vulnerabilities:* {{vulnerabilities}}\\n*Code Smells:* {{codeSmells}}\\n*Coverage:* {{coverage}}%\"\n        }\n      }\n    ]\n  }\n}") },
    { title: "custom_rules.xml", language: "XML", badge: "🛡️ Exception Handling", badgeColor: "#007AB8", description: "Custom quality profiles. At WebQ, we create tailored rule sets for each project type.", lines: s("<profile>\n  <name>WebQ Java Production</name>\n  <language>java</language>\n  <rules>\n    <!-- Security: Critical -->\n    <rule>\n      <repositoryKey>java</repositoryKey>\n      <key>S2076</key> <!-- OS Command Injection -->\n      <severity>BLOCKER</severity>\n    </rule>\n    <rule>\n      <repositoryKey>java</repositoryKey>\n      <key>S2078</key> <!-- XSS -->\n      <severity>BLOCKER</severity>\n    </rule>\n    <!-- Custom: No System.out -->\n    <rule>\n      <repositoryKey>java</repositoryKey>\n      <key>S106</key>\n      <severity>MAJOR</severity>\n    </rule>\n  </rules>\n</profile>") },
    { title: "developer_guide.txt", language: "Text", badge: "👤 User-Friendly", badgeColor: "#0099CC", description: "IDE integration for developers. At WebQ, we enable SonarLint for real-time code quality feedback.", lines: s("// SonarLint IDE Integration\n\n1. Install SonarLint plugin\n   - IntelliJ: Marketplace → SonarLint\n   - VS Code: Extensions → SonarLint\n\n2. Connect to SonarQube server\n   Settings → SonarLint → Connected Mode\n   Server URL: https://sonar.webq.com\n   Token: Generate from My Account → Security\n\n3. Bind project\n   SonarLint → Project Binding → Select project\n\n4. Real-time feedback\n   - Issues highlighted in editor\n   - Fix suggestions inline\n   - Rule descriptions with examples\n\n5. Pre-commit check\n   Run: sonar-scanner before pushing") },
    { title: "multi_project.yml", language: "YAML", badge: "📐 Best Practice", badgeColor: "#1A6BAF", description: "Multi-project setup with portfolios. At WebQ, we organize SonarQube with project templates and portfolios.", lines: s("# SonarQube project template\nprojects:\n  - key: webq-order-service\n    name: Order Service\n    qualityGate: WebQ Production Gate\n    qualityProfile: WebQ Java Production\n    tags: [backend, java, microservice]\n\n  - key: webq-frontend\n    name: WebQ Frontend\n    qualityGate: WebQ Production Gate\n    qualityProfile: WebQ TypeScript\n    tags: [frontend, typescript, react]\n\nportfolios:\n  - name: WebQ Platform\n    projects:\n      - webq-order-service\n      - webq-frontend\n      - webq-api-gateway") },
    { title: "pr_decoration.yml", language: "YAML", badge: "🔄 Event-Driven", badgeColor: "#0085C2", description: "PR decoration for code review. At WebQ, we annotate pull requests with SonarQube findings.", lines: s("# SonarQube PR decoration config\nsonar.pullrequest.key=${PR_NUMBER}\nsonar.pullrequest.branch=${PR_BRANCH}\nsonar.pullrequest.base=${TARGET_BRANCH}\n\n# GitHub integration\nsonar.pullrequest.provider=GitHub\nsonar.pullrequest.github.repository=webq/order-service\n\n# PR comment shows:\n# ✅ Quality Gate passed\n# - 0 Bugs (A)\n# - 0 Vulnerabilities (A)\n# - 2 Code Smells (A)\n# - 87.3% Coverage\n# - 0.0% Duplication\n#\n# Inline comments on new issues\n# with fix suggestions") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#3378B5", description: "Code quality standards documentation. At WebQ, we document quality gates, profiles, and remediation guides.", lines: s("# Code Quality Standards\n\n## Quality Gate: Production\n| Metric | Threshold |\n|--------|----------|\n| Bugs | 0 new (A rating) |\n| Vulnerabilities | 0 new (A rating) |\n| Coverage | ≥ 80% on new code |\n| Duplication | ≤ 3% on new code |\n| Hotspots | 100% reviewed |\n\n## Workflow\n1. Developer: SonarLint in IDE\n2. PR: Automated scan + decoration\n3. Merge: Quality Gate must pass\n4. Main: Full analysis + portfolio update\n\n## Fix Priority\nBlocker → Critical → Major → Minor") },
  ],
  metrics: [
    { value: "#1", label: "Code Quality", detail: "Industry standard" },
    { value: "400K+", label: "Organizations", detail: "Global adoption" },
    { value: "30+", label: "Languages", detail: "Supported" },
    { value: "5,000+", label: "Rules", detail: "Built-in rules" },
    { value: "OWASP", label: "Top 10", detail: "Security coverage" },
    { value: "SonarLint", label: "IDE Plugin", detail: "Real-time feedback" },
    { value: "Open Source", label: "Community Edition", detail: "Free tier" },
    { value: "CI/CD", label: "Native Integration", detail: "All platforms" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 80, use: "Security scanning, compliance" },
    { industry: "Technology", adoption: 85, use: "Code quality gates" },
    { industry: "Healthcare", adoption: 70, use: "HIPAA code security" },
    { industry: "Government", adoption: 72, use: "NIST compliance scanning" },
    { industry: "Automotive", adoption: 65, use: "Safety-critical code quality" },
    { industry: "Retail", adoption: 60, use: "PCI code security" },
  ],
  webqCallout: "At WebQ, we enforce code quality with SonarQube quality gates, custom security profiles, and PR decoration — catching bugs and vulnerabilities before they reach production.",
};

export const snyk: TechSnippetData = {
  snippets: [
    { title: "snyk_config.yml", language: "YAML", badge: "🔒 Secure Code", badgeColor: "#0059B3", description: "Snyk policy for vulnerability management. At WebQ, we configure Snyk policies per project risk level.", lines: s("# .snyk policy file\nversion: v1.25.0\nignore:\n  'SNYK-JS-LODASH-590103':\n    - '*':\n        reason: No user input reaches vulnerable function\n        expires: 2024-12-31T00:00:00.000Z\npatch:\n  'SNYK-JS-MINIMIST-559764':\n    - express > minimist:\n        patched: '2024-01-15'\n\n# Severity thresholds\n# CLI: snyk test --severity-threshold=high\n# CI: Fail on high and critical only\n\n# Language support\n# snyk test (npm, pip, maven, gradle)\n# snyk container test (Docker)\n# snyk iac test (Terraform, K8s)") },
    { title: "ci_integration.yml", language: "YAML", badge: "⚡ Performance", badgeColor: "#0070C0", description: "CI/CD integration with caching. At WebQ, we run Snyk scans efficiently with delta reporting and caching.", lines: s("name: Security Scan\non: [pull_request]\njobs:\n  snyk:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Snyk Dependencies\n        uses: snyk/actions/node@master\n        with:\n          args: --severity-threshold=high\n        env:\n          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}\n      - name: Snyk Container\n        uses: snyk/actions/docker@master\n        with:\n          image: webq/order-service:${{ github.sha }}\n          args: --severity-threshold=high\n      - name: Snyk IaC\n        uses: snyk/actions/iac@master\n        with:\n          args: --severity-threshold=medium") },
    { title: "test_scan.sh", language: "Bash", badge: "🧪 Testing", badgeColor: "#0080CC", description: "Automated vulnerability testing. At WebQ, we validate zero critical vulnerabilities before deployment.", lines: s("#!/bin/bash\nset -e\n\necho '🔍 Running Snyk security scans...'\n\n# Test dependencies\nsnyk test --json > /tmp/snyk-deps.json\nCRIT=$(jq '.vulnerabilities | map(select(.severity==\"critical\")) | length' /tmp/snyk-deps.json)\nassert_eq \"$CRIT\" \"0\" \"Critical dependency vulnerabilities found\"\n\n# Test container\nsnyk container test webq/order-service:latest --json > /tmp/snyk-container.json\nCRIT=$(jq '.vulnerabilities | map(select(.severity==\"critical\")) | length' /tmp/snyk-container.json)\nassert_eq \"$CRIT\" \"0\" \"Critical container vulnerabilities found\"\n\n# Test IaC\nsnyk iac test terraform/ --json > /tmp/snyk-iac.json\nHIGH=$(jq '[.[] | .targetFileRelativePath] | length' /tmp/snyk-iac.json)\n\necho '✅ All security scans passed'") },
    { title: "webhook_monitor.py", language: "Python", badge: "📝 Logging", badgeColor: "#0090D6", description: "Snyk webhook for vulnerability tracking. At WebQ, we monitor new vulnerabilities and track remediation SLAs.", lines: s("from flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/snyk-webhook', methods=['POST'])\ndef handle_snyk_event():\n    event = request.json\n\n    if event['type'] == 'new-vuln':\n        vuln = event['data']\n        severity = vuln['severity']\n        package = vuln['package']\n\n        log_vulnerability({\n            'severity': severity,\n            'package': package,\n            'cve': vuln.get('identifiers', {}).get('CVE', []),\n            'fix_available': vuln.get('isUpgradable', False)\n        })\n\n        if severity in ['critical', 'high']:\n            create_jira_ticket(vuln)\n            notify_slack(vuln)\n\n    return {'status': 'ok'}") },
    { title: "auto_fix.sh", language: "Bash", badge: "🛡️ Exception Handling", badgeColor: "#007AB8", description: "Automated fix with PR creation. At WebQ, we auto-remediate vulnerabilities with Snyk fix PRs.", lines: s("#!/bin/bash\n# Auto-fix workflow\n\n# Generate fix\nsnyk fix --dry-run\n\n# If fixes available, create PR\nif snyk fix; then\n    git checkout -b \"snyk-fix-$(date +%Y%m%d)\"\n    git add package.json package-lock.json\n    git commit -m \"fix(security): Auto-fix Snyk vulnerabilities\n\nApplied upgrades:\n$(snyk fix --dry-run 2>&1 | grep 'Upgrade')\n\nRemaining issues:\n$(snyk test --json | jq '.vulnerabilities | length') vulnerabilities\"\n\n    gh pr create \\\n      --title \"Security: Auto-fix dependencies\" \\\n      --body \"Automated Snyk fix PR\" \\\n      --label \"security,automated\"\nfi") },
    { title: "sbom_generation.sh", language: "Bash", badge: "👤 User-Friendly", badgeColor: "#0099CC", description: "SBOM generation for compliance. At WebQ, we generate software bills of materials for supply chain security.", lines: s("#!/bin/bash\n# Generate SBOM with Snyk\n\n# CycloneDX format\nsnyk sbom --format=cyclonedx1.4+json \\\n  --org=webq \\\n  --file=package.json \\\n  > sbom-cyclonedx.json\n\n# SPDX format\nsnyk sbom --format=spdx2.3+json \\\n  --org=webq \\\n  --file=package.json \\\n  > sbom-spdx.json\n\n# Upload to dependency track\ncurl -X POST $DTRACK_URL/api/v1/bom \\\n  -H 'X-Api-Key: '$DTRACK_KEY \\\n  -F 'project=order-service' \\\n  -F 'bom=@sbom-cyclonedx.json'\n\necho 'SBOM generated and uploaded ✅'") },
    { title: "snyk_config.yml", language: "YAML", badge: "📐 Best Practice", badgeColor: "#1A6BAF", description: "Organization-wide Snyk settings. At WebQ, we configure consistent security policies across all projects.", lines: s("# .snyk organization settings\norg:\n  name: webq\n  severity_threshold: high\n  auto_fix_prs: true\n  pr_checks_fail_threshold: high\n\nprojects:\n  order-service:\n    test_frequency: daily\n    auto_remediation: true\n    ignore_policy: strict\n\n  frontend:\n    test_frequency: weekly\n    auto_remediation: true\n\n# License policy\nlicense_policy:\n  high_severity:\n    - GPL-3.0\n    - AGPL-3.0\n  medium_severity:\n    - LGPL-3.0") },
    { title: "monitor_dashboard.py", language: "Python", badge: "🔄 Event-Driven", badgeColor: "#0085C2", description: "Snyk API for security dashboard. At WebQ, we build custom security dashboards from Snyk API data.", lines: s("import requests\n\ndef get_org_security_posture():\n    headers = {'Authorization': f'token {SNYK_TOKEN}'}\n    response = requests.get(\n        f'{SNYK_API}/org/{ORG_ID}/issues',\n        headers=headers,\n        params={'severity': 'high,critical'}\n    )\n\n    issues = response.json()['issues']\n    return {\n        'total_critical': len([i for i in issues if i['severity'] == 'critical']),\n        'total_high': len([i for i in issues if i['severity'] == 'high']),\n        'fixable': len([i for i in issues if i['isFixable']]),\n        'projects_affected': len(set(i['project']['name'] for i in issues))\n    }") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#3378B5", description: "Security scanning documentation. At WebQ, we document scanning policies, SLAs, and remediation workflows.", lines: s("# Snyk Security Scanning\n\n## Scan Types\n| Type | Frequency | Threshold |\n|------|-----------|----------|\n| Dependencies | Every PR + daily | High |\n| Container | Every build | High |\n| IaC | Every PR | Medium |\n\n## Remediation SLAs\n- Critical: 24 hours\n- High: 7 days\n- Medium: 30 days\n\n## Workflow\n1. PR scan → Block on critical\n2. Daily monitor → Alert on new vulns\n3. Auto-fix PR → Review and merge\n4. SBOM → Upload to Dependency-Track") },
  ],
  metrics: [
    { value: "#1", label: "Developer Security", detail: "Shift-left leader" },
    { value: "2,500+", label: "Customers", detail: "Enterprise adoption" },
    { value: "100+", label: "Languages", detail: "Ecosystem support" },
    { value: "SBOM", label: "Generation", detail: "CycloneDX + SPDX" },
    { value: "Auto-Fix", label: "PRs", detail: "Automated remediation" },
    { value: "Container", label: "Scanning", detail: "Image security" },
    { value: "IaC", label: "Security", detail: "Terraform, K8s" },
    { value: "IDE", label: "Integration", detail: "Real-time scanning" },
  ],
  industries: [
    { industry: "Technology", adoption: 85, use: "DevSecOps, dependency scanning" },
    { industry: "Financial Services", adoption: 75, use: "Supply chain security" },
    { industry: "Healthcare", adoption: 65, use: "HIPAA code security" },
    { industry: "Retail", adoption: 60, use: "PCI compliance scanning" },
    { industry: "Government", adoption: 55, use: "SBOM requirements" },
    { industry: "Automotive", adoption: 50, use: "Supply chain security" },
  ],
  webqCallout: "At WebQ, we integrate Snyk across the SDLC — scanning dependencies, containers, and IaC with automated fix PRs and SBOM generation for complete supply chain security.",
};

export const owaspZap: TechSnippetData = {
  snippets: [
    { title: "scan_config.yml", language: "YAML", badge: "🔒 Secure Code", badgeColor: "#0059B3", description: "Authenticated scan configuration. At WebQ, we configure ZAP with authentication and session management.", lines: s("# ZAP automation framework config\nenv:\n  contexts:\n    - name: 'WebQ App'\n      urls: ['https://app.webq.com']\n      authentication:\n        method: 'form'\n        parameters:\n          loginUrl: 'https://app.webq.com/login'\n          loginRequestData: 'username={%username%}&password={%password%}'\n        verification:\n          method: 'response'\n          loggedInRegex: 'Dashboard'\n      users:\n        - name: 'test-user'\n          credentials:\n            username: '${ZAP_USERNAME}'\n            password: '${ZAP_PASSWORD}'") },
    { title: "api_scan.yml", language: "YAML", badge: "⚡ Performance", badgeColor: "#0070C0", description: "API-focused scanning with OpenAPI. At WebQ, we scan REST APIs using OpenAPI specs for targeted testing.", lines: s("# ZAP API scan with OpenAPI\njobs:\n  - type: openapi\n    parameters:\n      apiUrl: 'https://api.webq.com/openapi.json'\n      context: 'API Context'\n  - type: activeScan\n    parameters:\n      context: 'API Context'\n      policy: 'API-Scan-Policy'\n      maxRuleDurationInMins: 5\n      maxScanDurationInMins: 30\n  - type: report\n    parameters:\n      template: 'traditional-json'\n      reportDir: '/zap/reports/'\n      reportFile: 'api-scan-results'\n    risks:\n      - high\n      - medium") },
    { title: "ci_scan.yml", language: "YAML", badge: "🧪 Testing", badgeColor: "#0080CC", description: "CI/CD integration with ZAP. At WebQ, we run DAST scans in pipelines with pass/fail thresholds.", lines: s("name: DAST Security Scan\non:\n  schedule:\n    - cron: '0 2 * * 1' # Weekly Monday 2 AM\njobs:\n  zap-scan:\n    runs-on: ubuntu-latest\n    steps:\n      - name: ZAP Full Scan\n        uses: zaproxy/action-full-scan@v0.9.0\n        with:\n          target: 'https://staging.webq.com'\n          rules_file_name: '.zap/rules.tsv'\n          fail_action: 'true'\n          allow_issue_writing: 'true'\n      - name: Upload Report\n        uses: actions/upload-artifact@v4\n        with:\n          name: zap-report\n          path: report_html.html") },
    { title: "alert_filter.py", language: "Python", badge: "📝 Logging", badgeColor: "#0090D6", description: "Alert filtering and reporting. At WebQ, we parse ZAP results with custom severity mapping and tracking.", lines: s("import json\n\ndef parse_zap_report(report_path):\n    with open(report_path) as f:\n        report = json.load(f)\n\n    findings = []\n    for site in report['site']:\n        for alert in site.get('alerts', []):\n            findings.append({\n                'name': alert['name'],\n                'risk': alert['riskdesc'],\n                'confidence': alert['confidence'],\n                'cwe': alert.get('cweid'),\n                'urls_affected': len(alert.get('instances', [])),\n                'solution': alert.get('solution', ''),\n            })\n\n    critical = [f for f in findings if 'High' in f['risk']]\n    if critical:\n        notify_security_team(critical)\n    return findings") },
    { title: "custom_scan.py", language: "Python", badge: "🛡️ Exception Handling", badgeColor: "#007AB8", description: "Custom scan scripts with ZAP API. At WebQ, we write custom active scan rules for application-specific vulnerabilities.", lines: s("from zapv2 import ZAPv2\n\nzap = ZAPv2(apikey=API_KEY,\n    proxies={'http': 'http://zap:8080'})\n\ndef run_targeted_scan(target_url):\n    try:\n        # Spider the target\n        scan_id = zap.spider.scan(target_url)\n        while int(zap.spider.status(scan_id)) < 100:\n            time.sleep(2)\n\n        # Active scan\n        scan_id = zap.ascan.scan(target_url)\n        while int(zap.ascan.status(scan_id)) < 100:\n            time.sleep(5)\n\n        alerts = zap.core.alerts(baseurl=target_url)\n        high_alerts = [a for a in alerts if a['risk'] == 'High']\n        return high_alerts\n    except Exception as e:\n        logger.error(f'ZAP scan failed: {e}')\n        return []") },
    { title: "baseline.yml", language: "YAML", badge: "👤 User-Friendly", badgeColor: "#0099CC", description: "Baseline scan for quick checks. At WebQ, we run baseline scans on every deployment for fast feedback.", lines: s("# Quick baseline scan (passive only)\njobs:\n  - type: spider\n    parameters:\n      context: 'Quick Check'\n      maxDuration: 5\n  - type: spiderAjax\n    parameters:\n      context: 'Quick Check'\n      maxDuration: 5\n  - type: passiveScan-wait\n    parameters:\n      maxDuration: 10\n  - type: report\n    parameters:\n      template: 'traditional-html'\n      reportDir: '/reports/'\n      reportTitle: 'Baseline Scan - {{date}}'\n    risks:\n      - high\n      - medium\n      - low") },
    { title: "scan_policy.yml", language: "YAML", badge: "📐 Best Practice", badgeColor: "#1A6BAF", description: "Custom scan policies per environment. At WebQ, we tune scan policies for staging vs production scanning.", lines: s("# Staging: Full aggressive scan\nstaging_policy:\n  scanners:\n    - id: 40012  # XSS Reflected\n      strength: HIGH\n      threshold: LOW\n    - id: 40018  # SQL Injection\n      strength: INSANE\n      threshold: LOW\n    - id: 90019  # Server Side Include\n      strength: HIGH\n      threshold: MEDIUM\n\n# Production: Safe passive scan only\nproduction_policy:\n  scanners:\n    - id: 10015  # Incomplete HTTPS\n      strength: DEFAULT\n    - id: 10020  # X-Frame-Options\n      strength: DEFAULT\n    - id: 10021  # X-Content-Type-Options\n      strength: DEFAULT") },
    { title: "hook_script.py", language: "Python", badge: "🔄 Event-Driven", badgeColor: "#0085C2", description: "ZAP script hooks for custom logic. At WebQ, we extend ZAP with custom scripts for authentication token handling.", lines: s("# ZAP HTTP Sender Script\ndef sendingRequest(msg, initiator, helper):\n    \"\"\"Add auth token to all requests\"\"\"\n    if 'api.webq.com' in msg.getRequestHeader().getURI().toString():\n        token = get_fresh_token()\n        msg.getRequestHeader().setHeader(\n            'Authorization', f'Bearer {token}'\n        )\n\ndef responseReceived(msg, initiator, helper):\n    \"\"\"Detect custom error patterns\"\"\"\n    body = msg.getResponseBody().toString()\n    if 'stack_trace' in body.lower():\n        # Raise alert for information disclosure\n        helper.raiseAlert(\n            risk=2,  # Medium\n            name='Stack Trace Disclosure',\n            description='Server returned stack trace'\n        )") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#3378B5", description: "DAST scanning procedures. At WebQ, we document scanning schedules, policies, and remediation workflows.", lines: s("# OWASP ZAP Security Scanning\n\n## Scan Types\n| Type | When | Duration |\n|------|------|----------|\n| Baseline | Every deploy | 5 min |\n| API Scan | Weekly | 30 min |\n| Full Scan | Monthly | 2 hours |\n\n## OWASP Top 10 Coverage\n- A01: Broken Access Control ✅\n- A03: Injection ✅\n- A05: Security Misconfiguration ✅\n- A07: XSS ✅\n\n## Remediation SLAs\n- High: 7 days\n- Medium: 30 days\n- Low: 90 days") },
  ],
  metrics: [
    { value: "#1", label: "Open Source DAST", detail: "Most popular scanner" },
    { value: "OWASP", label: "Flagship Project", detail: "Community backed" },
    { value: "12K+", label: "GitHub Stars", detail: "Active development" },
    { value: "Free", label: "Open Source", detail: "No license cost" },
    { value: "API", label: "Automation", detail: "Full REST API" },
    { value: "CI/CD", label: "Integration", detail: "GitHub, GitLab, Jenkins" },
    { value: "HUD", label: "Browser Plugin", detail: "Interactive testing" },
    { value: "100+", label: "Scan Rules", detail: "Active + passive" },
  ],
  industries: [
    { industry: "Technology", adoption: 80, use: "Web app security testing" },
    { industry: "Financial Services", adoption: 72, use: "PCI DAST requirements" },
    { industry: "Government", adoption: 68, use: "FedRAMP scanning" },
    { industry: "Healthcare", adoption: 60, use: "HIPAA web app testing" },
    { industry: "E-commerce", adoption: 65, use: "PCI web app scanning" },
    { industry: "Education", adoption: 55, use: "Student portal security" },
  ],
  webqCallout: "At WebQ, we integrate OWASP ZAP into CI/CD for automated DAST scanning — with baseline checks on every deploy and full scans weekly, covering the OWASP Top 10.",
};

export const fortify: TechSnippetData = {
  snippets: [
    { title: "fortify_scan.sh", language: "Bash", badge: "🔒 Secure Code", badgeColor: "#0059B3", description: "Static analysis with custom rulesets. At WebQ, we configure Fortify SAST with organization-specific security rules.", lines: s("#!/bin/bash\n# Fortify SCA static analysis\n\n# Translate source code\nsourceanalyzer -b order-service \\\n  -jdk 17 \\\n  -cp \"lib/*.jar\" \\\n  src/main/java/**/*.java\n\n# Scan with custom rules\nsourceanalyzer -b order-service \\\n  -scan \\\n  -rules custom_rules.xml \\\n  -f results/scan-$(date +%Y%m%d).fpr\n\n# Generate report\nReportGenerator \\\n  -format pdf \\\n  -f results/report.pdf \\\n  -source results/scan-$(date +%Y%m%d).fpr \\\n  -template WebQSecurityReport.xml") },
    { title: "ci_integration.yml", language: "YAML", badge: "⚡ Performance", badgeColor: "#0070C0", description: "Incremental scanning for fast CI feedback. At WebQ, we use Fortify's incremental scan mode for PR checks.", lines: s("name: Fortify SAST\non: [pull_request]\njobs:\n  fortify-scan:\n    runs-on: [self-hosted, fortify]\n    steps:\n      - uses: actions/checkout@v4\n      - name: Fortify Scan\n        uses: fortify/github-action@v1\n        with:\n          ssc-url: ${{ secrets.SSC_URL }}\n          ssc-token: ${{ secrets.SSC_TOKEN }}\n          ssc-appversion: order-service:main\n      - name: Check Results\n        run: |\n          CRITICAL=$(fortifyclient -url $SSC_URL \\\n            -authtoken $SSC_TOKEN \\\n            listIssues -search 'Critical' | wc -l)\n          if [ $CRITICAL -gt 0 ]; then\n            echo '❌ Critical vulnerabilities found'\n            exit 1\n          fi") },
    { title: "audit_test.py", language: "Python", badge: "🧪 Testing", badgeColor: "#0080CC", description: "Automated audit validation. At WebQ, we verify all critical findings are reviewed and dispositioned.", lines: s("import requests\n\ndef test_all_critical_reviewed():\n    response = requests.get(\n        f'{SSC_URL}/api/v1/projectVersions/{PV_ID}/issues',\n        headers={'Authorization': f'FortifyToken {TOKEN}'},\n        params={'q': 'friority:Critical AND audited:false'}\n    )\n    unreviewed = response.json()['data']\n    assert len(unreviewed) == 0, \\\n        f'{len(unreviewed)} critical issues not reviewed'\n\ndef test_no_new_critical():\n    response = requests.get(\n        f'{SSC_URL}/api/v1/projectVersions/{PV_ID}/issues',\n        params={'q': 'friority:Critical AND isNew:true'}\n    )\n    new_critical = response.json()['data']\n    assert len(new_critical) == 0") },
    { title: "ssc_dashboard.py", language: "Python", badge: "📝 Logging", badgeColor: "#0090D6", description: "SSC metrics for security dashboard. At WebQ, we track vulnerability trends and remediation rates.", lines: s("def get_security_metrics(project_version_id):\n    metrics = {}\n\n    # Issue counts by severity\n    response = requests.get(\n        f'{SSC_URL}/api/v1/projectVersions/{project_version_id}'\n        '/issueStatistics',\n        headers=auth_headers\n    )\n    stats = response.json()['data'][0]\n    metrics['critical'] = stats.get('critical', 0)\n    metrics['high'] = stats.get('high', 0)\n    metrics['medium'] = stats.get('medium', 0)\n\n    # Remediation rate\n    metrics['fixed_rate'] = (\n        stats.get('removedCount', 0) /\n        max(stats.get('totalCount', 1), 1)\n    ) * 100\n\n    return metrics") },
    { title: "custom_rules.xml", language: "XML", badge: "🛡️ Exception Handling", badgeColor: "#007AB8", description: "Custom Fortify rules for org-specific checks. At WebQ, we create custom rules for our coding standards.", lines: s("<RulePack>\n  <Name>WebQ Custom Rules</Name>\n  <Rules>\n    <RuleDefinition>\n      <RuleID>WEBQ-001</RuleID>\n      <Description>Unsafe deserialization detected</Description>\n      <Severity>Critical</Severity>\n      <Kingdom>Input Validation and Representation</Kingdom>\n      <Pattern>\n        <Source>\n          <Class>java.io.ObjectInputStream</Class>\n          <Method>readObject</Method>\n        </Source>\n      </Pattern>\n    </RuleDefinition>\n  </Rules>\n</RulePack>") },
    { title: "developer_workflow.txt", language: "Text", badge: "👤 User-Friendly", badgeColor: "#0099CC", description: "Developer-friendly IDE plugin. At WebQ, we enable Fortify IDE plugins for real-time security feedback.", lines: s("// Fortify Developer Workflow\n\n1. IDE Plugin (IntelliJ / VS Code)\n   - Real-time security analysis\n   - Inline fix suggestions\n   - Severity indicators in gutter\n\n2. Pre-commit Hook\n   sourceanalyzer -b project -quick-scan src/\n   # Quick scan: ~2 min for focused feedback\n\n3. Pull Request\n   - Full SAST scan\n   - Results posted as PR comments\n   - Critical findings block merge\n\n4. Audit Workflow\n   - Review in SSC web interface\n   - Disposition: FP / Not Issue / Must Fix\n   - Due dates based on severity") },
    { title: "ssc_config.json", language: "JSON", badge: "📐 Best Practice", badgeColor: "#1A6BAF", description: "SSC project template configuration. At WebQ, we standardize Fortify projects with templates and issue templates.", lines: s("{\n  \"projectTemplate\": {\n    \"name\": \"WebQ Standard\",\n    \"issueTemplates\": [\"PCI DSS\", \"OWASP Top 10\"],\n    \"customTags\": [\n      {\"name\": \"Business Unit\", \"type\": \"LIST\",\n       \"values\": [\"Orders\", \"Payments\", \"Auth\"]},\n      {\"name\": \"Risk Accepted By\", \"type\": \"TEXT\"},\n      {\"name\": \"Fix By Date\", \"type\": \"DATE\"}\n    ],\n    \"performanceIndicators\": [\n      {\"name\": \"Fortify Security Rating\"},\n      {\"name\": \"Days Since Last Scan\"}\n    ],\n    \"autoApplyTemplate\": true\n  }\n}") },
    { title: "dast_integration.sh", language: "Bash", badge: "🔄 Event-Driven", badgeColor: "#0085C2", description: "WebInspect DAST with SSC correlation. At WebQ, we correlate SAST and DAST findings in SSC for comprehensive coverage.", lines: s("#!/bin/bash\n# Fortify WebInspect DAST scan\n\n# Start scan\nWI_SCAN_ID=$(curl -X POST \\\n  \"$WI_URL/api/v2/scans\" \\\n  -H 'Authorization: FortifyToken ...' \\\n  -d '{\n    \"settingsName\": \"WebQ Standard\",\n    \"startUrls\": [\"https://staging.webq.com\"],\n    \"loginMacro\": \"webq_login.webmacro\"\n  }' | jq -r '.id')\n\n# Wait and upload to SSC\ncurl -X POST \\\n  \"$SSC_URL/api/v1/projectVersions/$PV_ID/artifacts\" \\\n  -F \"file=@scan-results.fpr\" \\\n  -H 'Authorization: FortifyToken ...'") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#3378B5", description: "SAST/DAST program documentation. At WebQ, we document the full application security testing program.", lines: s("# Application Security Testing\n\n## Fortify Coverage\n| Scan Type | Tool | Frequency |\n|-----------|------|----------|\n| SAST | Fortify SCA | Every PR |\n| DAST | WebInspect | Weekly |\n| SCA | Fortify SCA | Continuous |\n\n## Severity SLAs\n- Critical: 3 business days\n- High: 10 business days\n- Medium: 30 business days\n\n## Compliance\n- PCI DSS: Requirement 6.5\n- OWASP Top 10: Full coverage\n- NIST 800-53: SA-11\n\n## Contacts\n- AppSec Team: appsec@webq.com") },
  ],
  metrics: [
    { value: "#1", label: "Enterprise SAST", detail: "Gartner Leader" },
    { value: "OpenText", label: "Owned By", detail: "Micro Focus legacy" },
    { value: "30+", label: "Languages", detail: "Supported" },
    { value: "1,000+", label: "Vulnerability Categories", detail: "Comprehensive rules" },
    { value: "SSC", label: "Central Management", detail: "Software Security Center" },
    { value: "SAST + DAST", label: "Combined", detail: "Correlated findings" },
    { value: "PCI DSS", label: "Compliance", detail: "Req 6.5 mapping" },
    { value: "IDE", label: "Integration", detail: "IntelliJ, VS Code, Eclipse" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 82, use: "PCI SAST requirements" },
    { industry: "Government", adoption: 80, use: "FedRAMP, NIST compliance" },
    { industry: "Healthcare", adoption: 72, use: "HIPAA code security" },
    { industry: "Defense", adoption: 85, use: "Classified app security" },
    { industry: "Insurance", adoption: 68, use: "Regulatory compliance" },
    { industry: "Automotive", adoption: 60, use: "Safety-critical code analysis" },
  ],
  webqCallout: "At WebQ, we deploy Fortify for enterprise SAST/DAST — with custom rules, automated CI/CD integration, and SSC-based vulnerability management for comprehensive application security.",
};
