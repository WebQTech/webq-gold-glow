import { TechSnippetData } from "./technologySnippets";

const s = (c: string) => c.split("\n");

export const apacheKafka: TechSnippetData = {
  snippets: [
    { title: "security_config.properties", language: "Properties", badge: "🔒 Secure Code", badgeColor: "#f38ba8", description: "SASL/SSL authentication with ACLs. At WebQ, we enforce encrypted, authenticated Kafka clusters with topic-level authorization.", lines: s("# Broker security configuration\nlisteners=SASL_SSL://0.0.0.0:9093\nssl.keystore.location=/certs/kafka.keystore.jks\nssl.truststore.location=/certs/kafka.truststore.jks\nsasl.enabled.mechanisms=SCRAM-SHA-512\n\n# ACL authorization\nauthorizer.class.name=kafka.security.authorizer.AclAuthorizer\nsuper.users=User:admin\n\n# Producer ACL\n# kafka-acls --add --allow-principal User:order-service \\\n#   --operation Write --topic orders\n# Consumer ACL\n# kafka-acls --add --allow-principal User:analytics \\\n#   --operation Read --group analytics-group --topic orders") },
    { title: "producer_config.java", language: "Java", badge: "⚡ Performance", badgeColor: "#a6e3a1", description: "High-throughput producer with batching. At WebQ, we tune Kafka producers for maximum throughput with linger and compression.", lines: s("Properties props = new Properties();\nprops.put(\"bootstrap.servers\", \"kafka:9092\");\nprops.put(\"key.serializer\", StringSerializer.class);\nprops.put(\"value.serializer\", JsonSerializer.class);\n\n// Performance tuning\nprops.put(\"batch.size\", 32768);\nprops.put(\"linger.ms\", 20);\nprops.put(\"compression.type\", \"lz4\");\nprops.put(\"buffer.memory\", 67108864);\nprops.put(\"acks\", \"all\");\nprops.put(\"retries\", 3);\nprops.put(\"enable.idempotence\", true);\n\nKafkaProducer<String, Order> producer =\n    new KafkaProducer<>(props);") },
    { title: "consumer_test.java", language: "Java", badge: "🧪 Testing", badgeColor: "#89b4fa", description: "Embedded Kafka for integration testing. At WebQ, we test every consumer with EmbeddedKafka and assertion helpers.", lines: s("@EmbeddedKafka(topics = \"orders-test\")\n@SpringBootTest\nclass OrderConsumerTest {\n\n    @Autowired\n    private EmbeddedKafkaBroker kafka;\n\n    @Test\n    void consumesOrderEvent() {\n        Map<String, Object> props = KafkaTestUtils\n            .producerProps(kafka);\n        KafkaTemplate<String, String> template =\n            new KafkaTemplate<>(new DefaultKafkaProducerFactory<>(props));\n\n        template.send(\"orders-test\", \"{\\\"id\\\":1}\");\n\n        ConsumerRecord<String, String> record =\n            KafkaTestUtils.getSingleRecord(consumer, \"orders-test\");\n        assertThat(record.value()).contains(\"id\");\n    }\n}") },
    { title: "monitoring.java", language: "Java", badge: "📝 Logging", badgeColor: "#f9e2af", description: "JMX metrics with Prometheus exporter. At WebQ, we monitor consumer lag, throughput, and partition health on every cluster.", lines: s("// Kafka Streams metrics via JMX\nProperties streamsConfig = new Properties();\nstreamsConfig.put(StreamsConfig.METRICS_RECORDING_LEVEL_CONFIG,\n    \"DEBUG\");\nstreamsConfig.put(\"metric.reporters\",\n    \"io.prometheus.client.JmxCollector\");\n\n// Key metrics to monitor:\n// kafka.consumer:type=consumer-fetch-manager-metrics\n//   - records-lag-max\n//   - fetch-rate\n// kafka.producer:type=producer-metrics\n//   - record-send-rate\n//   - record-error-rate\n// kafka.server:type=BrokerTopicMetrics\n//   - MessagesInPerSec") },
    { title: "error_handler.java", language: "Java", badge: "🛡️ Exception Handling", badgeColor: "#cba6f7", description: "Dead letter queue for failed messages. At WebQ, we route poison pills to DLQ with retry logic and alerting.", lines: s("@Bean\npublic DefaultErrorHandler errorHandler(\n        KafkaTemplate<String, Object> template) {\n    DeadLetterPublishingRecoverer recoverer =\n        new DeadLetterPublishingRecoverer(template,\n            (record, ex) -> new TopicPartition(\n                record.topic() + \".DLQ\",\n                record.partition()));\n\n    DefaultErrorHandler handler = new DefaultErrorHandler(\n        recoverer,\n        new FixedBackOff(1000L, 3L) // 3 retries\n    );\n    handler.addNotRetryableExceptions(\n        DeserializationException.class);\n    return handler;\n}") },
    { title: "schema_registry.java", language: "Java", badge: "👤 User-Friendly", badgeColor: "#94e2d5", description: "Schema Registry for contract enforcement. At WebQ, we use Avro schemas with compatibility checks for safe evolution.", lines: s("// Producer with Schema Registry\nprops.put(\"schema.registry.url\", \"http://schema-registry:8081\");\nprops.put(\"value.serializer\",\n    KafkaAvroSerializer.class);\n\n// Avro schema for Order\n// {\n//   \"type\": \"record\",\n//   \"name\": \"Order\",\n//   \"fields\": [\n//     {\"name\": \"id\", \"type\": \"long\"},\n//     {\"name\": \"customer\", \"type\": \"string\"},\n//     {\"name\": \"total\", \"type\": \"double\"},\n//     {\"name\": \"status\", \"type\": {\n//       \"type\": \"enum\",\n//       \"symbols\": [\"PENDING\",\"SHIPPED\",\"DELIVERED\"]\n//     }}\n//   ]\n// }") },
    { title: "streams_topology.java", language: "Java", badge: "📐 Best Practice", badgeColor: "#fab387", description: "Kafka Streams topology for real-time processing. At WebQ, we build stateful stream processors with exactly-once semantics.", lines: s("StreamsBuilder builder = new StreamsBuilder();\n\nKStream<String, Order> orders = builder\n    .stream(\"orders\", Consumed.with(Serdes.String(), orderSerde));\n\n// Real-time aggregation\nKTable<String, OrderStats> stats = orders\n    .groupByKey()\n    .windowedBy(TimeWindows.ofSizeWithNoGrace(Duration.ofMinutes(5)))\n    .aggregate(\n        OrderStats::new,\n        (key, order, agg) -> agg.add(order),\n        Materialized.as(\"order-stats-store\")\n    );") },
    { title: "connect_sink.json", language: "JSON", badge: "🔄 Event-Driven", badgeColor: "#74c7ec", description: "Kafka Connect for CDC pipelines. At WebQ, we use Debezium connectors for change data capture across databases.", lines: s("{\n  \"name\": \"orders-sink\",\n  \"config\": {\n    \"connector.class\": \"io.debezium.connector.postgresql.PostgresConnector\",\n    \"database.hostname\": \"postgres\",\n    \"database.port\": \"5432\",\n    \"database.dbname\": \"orders\",\n    \"table.include.list\": \"public.orders\",\n    \"topic.prefix\": \"cdc\",\n    \"slot.name\": \"debezium_slot\",\n    \"plugin.name\": \"pgoutput\",\n    \"transforms\": \"unwrap\",\n    \"transforms.unwrap.type\":\n      \"io.debezium.transforms.ExtractNewRecordState\"\n  }\n}") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#f5c2e7", description: "Kafka architecture documentation. At WebQ, we document topic schemas, consumer groups, and data flow diagrams.", lines: s("# Order Event Streaming Platform\n\n## Topics\n| Topic | Partitions | Retention | Schema |\n|-------|-----------|-----------|--------|\n| orders | 12 | 7 days | Avro v3 |\n| orders.DLQ | 3 | 30 days | Same |\n| order-stats | 6 | Compact | JSON |\n\n## Consumer Groups\n- analytics-group: Real-time dashboards\n- fulfillment-group: Order processing\n\n## Data Flow\nPostgres → Debezium → Kafka → Streams → Elasticsearch") },
  ],
  metrics: [
    { value: "27K+", label: "GitHub Stars", detail: "Apache Kafka repo" },
    { value: "80%+", label: "Fortune 100", detail: "Enterprise adoption" },
    { value: "7T+", label: "Messages/Day", detail: "LinkedIn scale" },
    { value: "#1", label: "Event Streaming", detail: "Industry standard" },
    { value: "Confluent", label: "Commercial Platform", detail: "Enterprise support" },
    { value: "Sub-ms", label: "Latency", detail: "End-to-end" },
    { value: "PB-Scale", label: "Data Handling", detail: "Daily throughput" },
    { value: "10K+", label: "Companies", detail: "Production usage" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 85, use: "Real-time transactions, fraud detection" },
    { industry: "E-commerce", adoption: 80, use: "Order events, inventory sync" },
    { industry: "Telecommunications", adoption: 75, use: "CDR processing, network events" },
    { industry: "Technology", adoption: 88, use: "Microservices communication" },
    { industry: "Healthcare", adoption: 60, use: "HL7/FHIR event streaming" },
    { industry: "Logistics", adoption: 70, use: "Shipment tracking, fleet telemetry" },
  ],
  webqCallout: "At WebQ, we architect Kafka-based event streaming platforms with exactly-once semantics, Schema Registry governance, and Debezium CDC — powering real-time data flows at enterprise scale.",
};

export const apacheAirflow: TechSnippetData = {
  snippets: [
    { title: "secure_dag.py", language: "Python", badge: "🔒 Secure Code", badgeColor: "#f38ba8", description: "Secrets backend with connection encryption. At WebQ, we use Airflow's secrets backend to manage credentials without hardcoding.", lines: s("from airflow.decorators import dag, task\nfrom airflow.hooks.base import BaseHook\nfrom datetime import datetime\n\n@dag(schedule='@daily', start_date=datetime(2024,1,1),\n     catchup=False)\ndef secure_etl():\n    @task\n    def extract():\n        conn = BaseHook.get_connection('postgres_prod')\n        # Connection stored in secrets backend\n        # Never hardcode credentials in DAG files\n        engine = create_engine(\n            f'postgresql://{conn.login}:{conn.password}'\n            f'@{conn.host}:{conn.port}/{conn.schema}'\n        )\n        return engine.execute('SELECT * FROM orders').fetchall()") },
    { title: "optimized_dag.py", language: "Python", badge: "⚡ Performance", badgeColor: "#a6e3a1", description: "Task parallelism with pool and priority. At WebQ, we optimize DAG execution with pools, priorities, and dynamic task mapping.", lines: s("@dag(schedule='@hourly', max_active_runs=3)\ndef optimized_pipeline():\n    @task(pool='db_pool', priority_weight=10)\n    def extract_orders():\n        return query_database('orders')\n\n    @task(pool='db_pool', priority_weight=5)\n    def extract_customers():\n        return query_database('customers')\n\n    @task.expand(partition=range(10))\n    def process_partition(partition, orders, customers):\n        # Dynamic task mapping for parallelism\n        return transform(orders, customers, partition)\n\n    orders = extract_orders()\n    customers = extract_customers()\n    process_partition(orders=orders, customers=customers)") },
    { title: "test_dag.py", language: "Python", badge: "🧪 Testing", badgeColor: "#89b4fa", description: "DAG validation and task testing. At WebQ, we test every DAG for import errors and validate task dependencies.", lines: s("import pytest\nfrom airflow.models import DagBag\n\n@pytest.fixture\ndef dagbag():\n    return DagBag(dag_folder='dags/', include_examples=False)\n\ndef test_no_import_errors(dagbag):\n    assert len(dagbag.import_errors) == 0, \\\n        f'DAG import errors: {dagbag.import_errors}'\n\ndef test_etl_dag_structure(dagbag):\n    dag = dagbag.get_dag('secure_etl')\n    assert dag is not None\n    tasks = dag.task_ids\n    assert 'extract' in tasks\n    assert 'transform' in tasks\n    assert 'load' in tasks\n\ndef test_task_dependencies(dagbag):\n    dag = dagbag.get_dag('secure_etl')\n    extract = dag.get_task('extract')\n    assert 'transform' in [t.task_id for t in extract.downstream_list]") },
    { title: "logging_config.py", language: "Python", badge: "📝 Logging", badgeColor: "#f9e2af", description: "Structured logging with task context. At WebQ, we enrich Airflow logs with run metadata for debugging pipelines.", lines: s("import logging\nfrom airflow.decorators import task\n\nlogger = logging.getLogger(__name__)\n\n@task\ndef transform(data, **context):\n    run_id = context['run_id']\n    ti = context['ti']\n    logger.info(f'Transform started | run={run_id} | '\n                f'task={ti.task_id} | attempt={ti.try_number}')\n\n    try:\n        result = process(data)\n        logger.info(f'Processed {len(result)} records')\n        return result\n    except Exception as e:\n        logger.error(f'Transform failed: {e}',\n                     exc_info=True)\n        raise") },
    { title: "error_callback.py", language: "Python", badge: "🛡️ Exception Handling", badgeColor: "#cba6f7", description: "SLA alerts and failure callbacks. At WebQ, we configure on_failure_callback for automated incident routing.", lines: s("from airflow.decorators import dag\nfrom airflow.operators.python import PythonOperator\n\ndef on_failure(context):\n    task = context['task_instance']\n    send_alert(\n        channel='#data-alerts',\n        message=f'❌ DAG {task.dag_id} failed\\n'\n                f'Task: {task.task_id}\\n'\n                f'Run: {task.run_id}\\n'\n                f'Error: {context.get(\"exception\")}'\n    )\n\n@dag(on_failure_callback=on_failure,\n     sla_miss_callback=on_sla_miss,\n     default_args={'retries': 2,\n                   'retry_delay': timedelta(minutes=5)})\ndef monitored_pipeline():\n    pass") },
    { title: "data_quality.py", language: "Python", badge: "👤 User-Friendly", badgeColor: "#94e2d5", description: "Data quality checks with Great Expectations. At WebQ, we validate data quality at every stage of the pipeline.", lines: s("from airflow.decorators import task\nfrom great_expectations_provider.operators.great_expectations \\\n    import GreatExpectationsOperator\n\n@task\ndef validate_orders():\n    return GreatExpectationsOperator(\n        task_id='validate_orders',\n        data_context_root_dir='great_expectations/',\n        checkpoint_name='orders_checkpoint',\n        fail_task_on_validation_failure=True\n    )\n\n# Checkpoint validates:\n# - No null order IDs\n# - Total > 0\n# - Valid status values\n# - Date not in future") },
    { title: "dag_factory.py", language: "Python", badge: "📐 Best Practice", badgeColor: "#fab387", description: "DAG factory pattern for standardization. At WebQ, we generate DAGs from YAML configs for consistent pipeline structure.", lines: s("# dag_config.yml\n# pipelines:\n#   - name: orders_etl\n#     schedule: '@daily'\n#     source: postgres.orders\n#     destination: snowflake.analytics\n\nimport yaml\nfrom airflow import DAG\n\ndef create_dag(config):\n    with DAG(dag_id=config['name'],\n             schedule=config['schedule'],\n             default_args=DEFAULT_ARGS) as dag:\n        extract = create_extract_task(config['source'])\n        transform = create_transform_task(config)\n        load = create_load_task(config['destination'])\n        extract >> transform >> load\n    return dag\n\nfor cfg in load_configs():\n    globals()[cfg['name']] = create_dag(cfg)") },
    { title: "event_trigger.py", language: "Python", badge: "🔄 Event-Driven", badgeColor: "#74c7ec", description: "Event-driven DAG triggering with sensors. At WebQ, we use sensors and dataset-aware scheduling for reactive pipelines.", lines: s("from airflow.sensors.filesystem import FileSensor\nfrom airflow.datasets import Dataset\n\norders_dataset = Dataset('s3://data/orders/')\n\n@dag(schedule=[orders_dataset])\ndef downstream_analytics():\n    # Triggered when orders dataset is updated\n    @task(outlets=[Dataset('s3://data/reports/')])\n    def generate_report():\n        pass\n\n# File sensor for external data\nwait_for_file = FileSensor(\n    task_id='wait_for_export',\n    filepath='/data/exports/daily_*.csv',\n    poke_interval=300,\n    timeout=3600,\n    mode='reschedule'  # Free worker while waiting\n)") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#f5c2e7", description: "Pipeline documentation with lineage. At WebQ, we document every DAG with SLA, ownership, and data lineage.", lines: s("# Orders ETL Pipeline\n\n## Schedule: Daily at 2:00 AM UTC\n## Owner: Data Engineering Team\n## SLA: Complete within 2 hours\n\n## Data Flow\nPostgres → Extract → Validate → Transform → Snowflake\n\n## Dependencies\n- Upstream: `raw_orders_ingestion`\n- Downstream: `analytics_refresh`\n\n## Alerts\n- Slack: #data-alerts\n- PagerDuty: data-oncall\n\n## Runbook\n1. Check task logs in Airflow UI\n2. Verify source DB connectivity\n3. Re-run from failed task") },
  ],
  metrics: [
    { value: "37K+", label: "GitHub Stars", detail: "Apache Airflow repo" },
    { value: "#1", label: "Workflow Orchestrator", detail: "Industry standard" },
    { value: "10K+", label: "Companies", detail: "Production usage" },
    { value: "2,000+", label: "Contributors", detail: "Open-source community" },
    { value: "Astronomer", label: "Commercial Platform", detail: "Managed Airflow" },
    { value: "Python", label: "DAG Definition", detail: "Familiar language" },
    { value: "1,000+", label: "Providers", detail: "Integration ecosystem" },
    { value: "70%+", label: "Data Teams Use It", detail: "Industry survey 2024" },
  ],
  industries: [
    { industry: "E-commerce", adoption: 80, use: "Order ETL, inventory sync" },
    { industry: "Financial Services", adoption: 75, use: "Regulatory reporting, reconciliation" },
    { industry: "Technology", adoption: 85, use: "Data platform orchestration" },
    { industry: "Healthcare", adoption: 65, use: "Clinical data pipelines" },
    { industry: "Media", adoption: 70, use: "Content analytics, recommendation data" },
    { industry: "Telecommunications", adoption: 60, use: "CDR processing, billing" },
  ],
  webqCallout: "At WebQ, we orchestrate complex data pipelines with Airflow — using DAG factories, data quality gates, and event-driven scheduling for reliable, observable ETL workflows.",
};

export const dbt: TechSnippetData = {
  snippets: [
    { title: "grants.sql", language: "SQL", badge: "🔒 Secure Code", badgeColor: "#f38ba8", description: "Model-level grants for access control. At WebQ, we enforce role-based access on every dbt model.", lines: s("-- models/marts/orders.sql\n{{ config(\n    materialized='table',\n    grants={\n        'select': ['analytics_role', 'bi_role'],\n    },\n    tags=['pii', 'financial']\n) }}\n\nSELECT\n    order_id,\n    {{ dbt_utils.star(\n        from=ref('stg_orders'),\n        except=['customer_ssn', 'credit_card']\n    ) }},\n    -- Mask PII in non-prod\n    {% if target.name != 'prod' %}\n        md5(customer_email) as customer_email\n    {% else %}\n        customer_email\n    {% endif %}\nFROM {{ ref('stg_orders') }}") },
    { title: "incremental_model.sql", language: "SQL", badge: "⚡ Performance", badgeColor: "#a6e3a1", description: "Incremental models for large tables. At WebQ, we optimize dbt runs with incremental strategy and merge keys.", lines: s("-- models/marts/fact_orders.sql\n{{ config(\n    materialized='incremental',\n    unique_key='order_id',\n    incremental_strategy='merge',\n    cluster_by=['order_date', 'region']\n) }}\n\nSELECT\n    o.order_id,\n    o.customer_id,\n    o.total,\n    o.order_date,\n    c.region\nFROM {{ ref('stg_orders') }} o\nJOIN {{ ref('dim_customers') }} c USING (customer_id)\n\n{% if is_incremental() %}\n    WHERE o.updated_at > (\n        SELECT max(updated_at) FROM {{ this }}\n    )\n{% endif %}") },
    { title: "schema_test.yml", language: "YAML", badge: "🧪 Testing", badgeColor: "#89b4fa", description: "Data tests for quality assurance. At WebQ, we test every model with not_null, unique, and custom tests.", lines: s("# models/marts/schema.yml\nmodels:\n  - name: fact_orders\n    description: 'Completed orders fact table'\n    columns:\n      - name: order_id\n        tests:\n          - not_null\n          - unique\n      - name: total\n        tests:\n          - not_null\n          - dbt_utils.accepted_range:\n              min_value: 0\n              max_value: 100000\n      - name: order_date\n        tests:\n          - not_null\n          - dbt_utils.not_accepted_values:\n              values: ['1970-01-01']") },
    { title: "logging_macro.sql", language: "SQL", badge: "📝 Logging", badgeColor: "#f9e2af", description: "Custom logging macros for audit trails. At WebQ, we log model run metadata for pipeline observability.", lines: s("-- macros/log_model_run.sql\n{% macro log_model_run() %}\n    {% set query %}\n        INSERT INTO {{ ref('audit_log') }} (\n            model_name,\n            run_started_at,\n            run_id,\n            target_name,\n            row_count\n        ) VALUES (\n            '{{ this.name }}',\n            '{{ run_started_at }}',\n            '{{ invocation_id }}',\n            '{{ target.name }}',\n            (SELECT count(*) FROM {{ this }})\n        )\n    {% endset %}\n    {% do run_query(query) %}\n{% endmacro %}") },
    { title: "custom_test.sql", language: "SQL", badge: "🛡️ Exception Handling", badgeColor: "#cba6f7", description: "Custom generic tests for data validation. At WebQ, we build reusable tests for domain-specific business rules.", lines: s("-- tests/generic/test_revenue_reconciliation.sql\n{% test revenue_reconciliation(model, column_name) %}\n\nWITH\nmodel_total AS (\n    SELECT SUM({{ column_name }}) AS total\n    FROM {{ model }}\n    WHERE order_date = CURRENT_DATE - 1\n),\nsource_total AS (\n    SELECT SUM(amount) AS total\n    FROM {{ source('raw', 'payments') }}\n    WHERE payment_date = CURRENT_DATE - 1\n)\nSELECT *\nFROM model_total, source_total\nWHERE ABS(model_total.total - source_total.total) > 1.00\n\n{% endtest %}") },
    { title: "docs_block.md", language: "SQL", badge: "👤 User-Friendly", badgeColor: "#94e2d5", description: "dbt docs with markdown descriptions. At WebQ, we generate interactive data catalogs from dbt documentation.", lines: s("-- models/marts/schema.yml\nmodels:\n  - name: dim_customers\n    description: >\n      Customer dimension table enriched with\n      lifetime value and segmentation.\n      Updated daily via incremental load.\n\n    meta:\n      owner: 'data-team'\n      contains_pii: true\n      sla: '6:00 AM UTC'\n\n    columns:\n      - name: customer_id\n        description: 'Unique customer identifier (UUID)'\n      - name: lifetime_value\n        description: 'Total revenue from customer (USD)'\n      - name: segment\n        description: 'RFM segment: premium/standard/basic'") },
    { title: "project_structure.yml", language: "YAML", badge: "📐 Best Practice", badgeColor: "#fab387", description: "Project structure with staging/marts layers. At WebQ, we follow the dbt best-practice layered architecture.", lines: s("# dbt_project.yml\nname: webq_analytics\nmodels:\n  webq_analytics:\n    staging:\n      +materialized: view\n      +schema: staging\n      +tags: ['staging']\n    intermediate:\n      +materialized: ephemeral\n    marts:\n      +materialized: table\n      +schema: analytics\n      +tags: ['marts']\n      +grants:\n        select: ['bi_role']\n\n# Folder structure:\n# models/\n#   staging/    (1:1 with sources)\n#   intermediate/  (business logic)\n#   marts/      (final tables)") },
    { title: "source_freshness.yml", language: "YAML", badge: "🔄 Event-Driven", badgeColor: "#74c7ec", description: "Source freshness monitoring. At WebQ, we validate source data freshness before running transformations.", lines: s("# models/staging/sources.yml\nsources:\n  - name: raw\n    database: raw_db\n    freshness:\n      warn_after: {count: 12, period: hour}\n      error_after: {count: 24, period: hour}\n    loaded_at_field: _loaded_at\n    tables:\n      - name: orders\n        identifier: raw_orders\n        freshness:\n          warn_after: {count: 1, period: hour}\n          error_after: {count: 3, period: hour}\n      - name: customers\n      - name: products\n\n# Run: dbt source freshness") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#f5c2e7", description: "dbt project documentation. At WebQ, we maintain comprehensive docs with DAG lineage and data dictionary.", lines: s("# Analytics Data Models\n\n## Architecture\n```\nRaw Sources → Staging → Intermediate → Marts\n```\n\n## Key Models\n| Model | Type | Description |\n|-------|------|-------------|\n| fact_orders | Table | Order facts |\n| dim_customers | Table | Customer dimension |\n\n## Commands\n```bash\ndbt run --select marts   # Build marts\ndbt test                 # Run all tests\ndbt docs generate        # Generate docs\ndbt source freshness     # Check sources\n```") },
  ],
  metrics: [
    { value: "9K+", label: "GitHub Stars", detail: "dbt-core repo" },
    { value: "#1", label: "Analytics Engineering", detail: "Industry standard" },
    { value: "30K+", label: "Companies", detail: "dbt Cloud users" },
    { value: "SQL-Based", label: "Approach", detail: "Analyst-friendly" },
    { value: "5,000+", label: "dbt Packages", detail: "Hub ecosystem" },
    { value: "60K+", label: "Community Members", detail: "dbt Slack" },
    { value: "Testing", label: "Built-in", detail: "Data quality" },
    { value: "Docs", label: "Auto-Generated", detail: "Data catalog" },
  ],
  industries: [
    { industry: "Technology", adoption: 85, use: "Product analytics, data modeling" },
    { industry: "E-commerce", adoption: 78, use: "Revenue analytics, customer 360" },
    { industry: "Financial Services", adoption: 70, use: "Regulatory reporting" },
    { industry: "Healthcare", adoption: 60, use: "Clinical analytics" },
    { industry: "SaaS", adoption: 82, use: "Product metrics, ARR tracking" },
    { industry: "Media", adoption: 68, use: "Content engagement analytics" },
  ],
  webqCallout: "At WebQ, we build dbt projects with layered architecture, comprehensive testing, and auto-generated documentation — delivering trustworthy, governed analytics data models.",
};

export const tableau: TechSnippetData = {
  snippets: [
    { title: "row_level_security.sql", language: "SQL", badge: "🔒 Secure Code", badgeColor: "#f38ba8", description: "Row-level security with user filters. At WebQ, we enforce data access policies at the workbook level.", lines: s("-- Tableau row-level security via entitlement table\nCREATE TABLE data_entitlements (\n    username VARCHAR(100),\n    region VARCHAR(50),\n    access_level VARCHAR(20)\n);\n\n-- In Tableau calculated field:\n-- IF ISMEMBEROF('Admins') THEN TRUE\n-- ELSEIF [Region] = USERNAME() THEN TRUE\n-- ELSE FALSE\n-- END\n\n-- Data source filter applied:\n-- [Has Access] = TRUE\n\n-- Published data source with\n-- embedded credentials and\n-- row-level security enabled") },
    { title: "performance_tips.txt", language: "Text", badge: "⚡ Performance", badgeColor: "#a6e3a1", description: "Extract optimization and query performance. At WebQ, we optimize Tableau with extracts, aggregations, and data source design.", lines: s("// Tableau Performance Optimization\n\n1. Use Extracts over Live connections\n   - Hyper engine for fast queries\n   - Schedule incremental refresh\n\n2. Reduce data volume\n   - Filter at data source level\n   - Aggregate to required grain\n   - Hide unused fields\n\n3. Dashboard design\n   - Limit sheets per dashboard to 6-8\n   - Use context filters first\n   - Avoid FIXED LOD when possible\n\n4. Calculated fields\n   - Use native functions over custom SQL\n   - Pre-compute in database\n   - Avoid row-level string calculations") },
    { title: "test_workbook.py", language: "Python", badge: "🧪 Testing", badgeColor: "#89b4fa", description: "Tableau Server REST API for automated testing. At WebQ, we validate published workbooks with API-based health checks.", lines: s("import tableauserverclient as TSC\n\ndef test_workbook_published():\n    auth = TSC.TableauAuth('admin', 'password', 'default')\n    server = TSC.Server('https://tableau.webq.com')\n    with server.auth.sign_in(auth):\n        workbooks, _ = server.workbooks.get()\n        names = [wb.name for wb in workbooks]\n        assert 'Revenue Dashboard' in names\n\ndef test_data_freshness():\n    with server.auth.sign_in(auth):\n        wb = server.workbooks.get_by_id(wb_id)\n        assert wb.updated_at > datetime.now() - timedelta(hours=24),\\\n            'Workbook data is stale'") },
    { title: "audit_logs.py", language: "Python", badge: "📝 Logging", badgeColor: "#f9e2af", description: "Tableau activity monitoring with Admin Views. At WebQ, we monitor dashboard usage and query performance.", lines: s("-- Tableau Server Admin View queries\n-- Built-in PostgreSQL repository\n\nSELECT\n    vu.name AS user_name,\n    v.name AS view_name,\n    vs.nviews AS view_count,\n    vs.last_view_time\nFROM _views_stats vs\nJOIN _views v ON vs.view_id = v.id\nJOIN _users vu ON vs.user_id = vu.id\nWHERE vs.last_view_time > NOW() - INTERVAL '30 days'\nORDER BY vs.nviews DESC;\n\n-- Monitor slow queries\nSELECT view_name, avg_duration_ms\nFROM _http_requests\nWHERE action = 'bootstrapSession'\n  AND avg_duration_ms > 10000;") },
    { title: "error_handling.py", language: "Python", badge: "🛡️ Exception Handling", badgeColor: "#cba6f7", description: "Automated extract refresh monitoring. At WebQ, we detect and alert on failed Tableau extract refreshes.", lines: s("import tableauserverclient as TSC\n\ndef check_extract_failures():\n    with server.auth.sign_in(auth):\n        tasks, _ = server.tasks.get()\n        failed = [t for t in tasks\n                  if t.task_type == 'RefreshExtract'\n                  and t.latest_status == 'Failed']\n\n        for task in failed:\n            send_alert(\n                channel='#bi-alerts',\n                message=f'❌ Extract failed: {task.target.name}\\n'\n                        f'Error: {task.latest_status_notes}\\n'\n                        f'Schedule: {task.schedule_id}'\n            )\n        return len(failed) == 0") },
    { title: "design_guide.txt", language: "Text", badge: "👤 User-Friendly", badgeColor: "#94e2d5", description: "Dashboard design principles. At WebQ, we follow Tableau best practices for intuitive, accessible dashboards.", lines: s("// WebQ Tableau Design Standards\n\n1. Layout\n   - Fixed size: 1366x768 (most common)\n   - Navigation bar at top\n   - Filters in sidebar\n   - KPIs above detailed views\n\n2. Color\n   - Use WebQ brand palette\n   - Max 5-7 colors per view\n   - Color-blind friendly palette\n   - Gray for context, color for focus\n\n3. Interactivity\n   - Filter actions (not highlight)\n   - Tooltips with context\n   - Drill-down navigation\n   - Export buttons for users") },
    { title: "lod_expressions.txt", language: "Text", badge: "📐 Best Practice", badgeColor: "#fab387", description: "LOD expressions for complex analytics. At WebQ, we use FIXED/INCLUDE/EXCLUDE for multi-level aggregations.", lines: s("// Level of Detail Expressions\n\n// Customer lifetime value\n{ FIXED [Customer ID] : SUM([Revenue]) }\n\n// Average order value by region\n{ FIXED [Region] : AVG(\n    { FIXED [Order ID] : SUM([Amount]) }\n)}\n\n// Year-over-year comparison\nSUM([Revenue]) -\nLOOKUP(SUM([Revenue]), -1)\n\n// Running total with table calc\nRUNNING_SUM(SUM([Revenue]))\n\n// Percent of total\nSUM([Revenue]) / TOTAL(SUM([Revenue]))") },
    { title: "api_automation.py", language: "Python", badge: "🔄 Event-Driven", badgeColor: "#74c7ec", description: "Tableau REST API for automated publishing. At WebQ, we automate workbook deployment across environments.", lines: s("import tableauserverclient as TSC\n\ndef publish_workbook(workbook_path, project_name):\n    auth = TSC.TableauAuth(user, password, site)\n    server = TSC.Server(server_url, use_server_version=True)\n\n    with server.auth.sign_in(auth):\n        project = get_project(server, project_name)\n        publish_mode = TSC.Server.PublishMode.Overwrite\n\n        wb = TSC.WorkbookItem(project.id, name='Revenue')\n        wb = server.workbooks.publish(\n            wb, workbook_path, publish_mode,\n            connections=[connection]\n        )\n        print(f'Published: {wb.webpage_url}')") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#f5c2e7", description: "Dashboard documentation standards. At WebQ, we document every Tableau workbook with data sources and business context.", lines: s("# Revenue Analytics Dashboard\n\n## Data Sources\n- Snowflake: analytics.fact_orders\n- Refresh: Daily at 6:00 AM UTC\n\n## KPIs\n| Metric | Calculation | Target |\n|--------|------------|--------|\n| Revenue | SUM(total) | $1M/mo |\n| AOV | AVG(order_total) | $85 |\n| Conversion | Orders/Visits | 3.5% |\n\n## Filters\n- Date range (default: last 30 days)\n- Region, Product category\n\n## Access\n- Group: Analytics Team, Leadership") },
  ],
  metrics: [
    { value: "#1", label: "BI Platform", detail: "Gartner Magic Quadrant" },
    { value: "100K+", label: "Organizations", detail: "Global customers" },
    { value: "Salesforce", label: "Owned By", detail: "Enterprise backing" },
    { value: "2M+", label: "Community Members", detail: "Tableau Public" },
    { value: "Visual", label: "Drag-and-Drop", detail: "No-code analytics" },
    { value: "Hyper", label: "Engine", detail: "Fast extracts" },
    { value: "Prep", label: "Data Cleaning", detail: "Built-in ETL" },
    { value: "Mobile", label: "Native Apps", detail: "iOS & Android" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 82, use: "Risk dashboards, regulatory reporting" },
    { industry: "Healthcare", adoption: 72, use: "Clinical analytics, population health" },
    { industry: "Retail", adoption: 78, use: "Sales analytics, inventory dashboards" },
    { industry: "Technology", adoption: 75, use: "Product metrics, user analytics" },
    { industry: "Government", adoption: 65, use: "Open data portals, performance metrics" },
    { industry: "Education", adoption: 60, use: "Student analytics, enrollment tracking" },
  ],
  webqCallout: "At WebQ, we build Tableau solutions with optimized data architecture, row-level security, and governed self-service — delivering intuitive analytics that drive business decisions.",
};

export const powerBi: TechSnippetData = {
  snippets: [
    { title: "rls_setup.dax", language: "DAX", badge: "🔒 Secure Code", badgeColor: "#f38ba8", description: "Row-level security with DAX filters. At WebQ, we implement dynamic RLS with Azure AD group mappings.", lines: s("[Security Role: RegionalManager]\nTable: fact_sales\nDAX Filter:\n  [Region] = LOOKUPVALUE(\n    dim_users[Region],\n    dim_users[Email],\n    USERPRINCIPALNAME()\n  )\n\n[Security Role: DepartmentHead]\nTable: fact_sales\nDAX Filter:\n  PATHCONTAINS(\n    LOOKUPVALUE(\n      dim_org[OrgPath],\n      dim_org[ManagerEmail],\n      USERPRINCIPALNAME()\n    ),\n    [Department_ID]\n  )") },
    { title: "performance_dax.dax", language: "DAX", badge: "⚡ Performance", badgeColor: "#a6e3a1", description: "Optimized DAX measures for fast rendering. At WebQ, we write storage-engine-friendly DAX for sub-second dashboards.", lines: s("// Optimized: Use variables to avoid recalculation\nRevenue MTD =\nVAR CurrentDate = MAX(dim_date[Date])\nVAR MonthStart = STARTOFMONTH(CurrentDate)\nRETURN\n    CALCULATE(\n        SUM(fact_sales[Revenue]),\n        DATESBETWEEN(dim_date[Date], MonthStart, CurrentDate)\n    )\n\n// Optimized: SUMMARIZE + ADDCOLUMNS pattern\nCategory Revenue =\nADDCOLUMNS(\n    SUMMARIZE(fact_sales, dim_product[Category]),\n    \"Revenue\", CALCULATE(SUM(fact_sales[Revenue]))\n)") },
    { title: "test_measures.py", language: "Python", badge: "🧪 Testing", badgeColor: "#89b4fa", description: "DAX measure testing with Tabular Editor. At WebQ, we validate measures against known datasets before publishing.", lines: s("# Power BI measure testing with pyadomd\nfrom pyadomd import Pyadomd\n\ndef test_revenue_measure():\n    conn_str = 'Provider=MSOLAP;Data Source=localhost:port'\n    with Pyadomd(conn_str) as conn:\n        cursor = conn.cursor()\n        cursor.execute(\n            'EVALUATE ROW(\"Revenue\", [Total Revenue])'\n        )\n        result = cursor.fetchone()[0]\n        assert result > 0, 'Revenue should be positive'\n\ndef test_yoy_calculation():\n    with Pyadomd(conn_str) as conn:\n        cursor = conn.cursor()\n        cursor.execute(\n            'EVALUATE ROW(\"YoY\", [Revenue YoY %])'\n        )\n        result = cursor.fetchone()[0]\n        assert -1 <= result <= 10, 'YoY out of range'") },
    { title: "audit_activity.ps1", language: "PowerShell", badge: "📝 Logging", badgeColor: "#f9e2af", description: "Power BI activity logging with REST API. At WebQ, we monitor report usage and refresh status for governance.", lines: s("# Get Power BI activity events\n$token = Get-PowerBIAccessToken\n$startDate = (Get-Date).AddDays(-1).ToString('yyyy-MM-ddTHH:mm:ss')\n$endDate = (Get-Date).ToString('yyyy-MM-ddTHH:mm:ss')\n\n$activities = Invoke-PowerBIRestMethod \\\n  -Url \"admin/activityevents?startDateTime='$startDate'&endDateTime='$endDate'\" \\\n  -Method Get\n\n$activities.activityEventEntities |\n  Where-Object { $_.Activity -eq 'ViewReport' } |\n  Group-Object ReportName |\n  Sort-Object Count -Descending |\n  Select-Object Count, Name -First 20") },
    { title: "error_monitoring.ps1", language: "PowerShell", badge: "🛡️ Exception Handling", badgeColor: "#cba6f7", description: "Automated refresh failure detection. At WebQ, we monitor dataset refresh status and route alerts for failures.", lines: s("# Monitor dataset refresh failures\n$datasets = Get-PowerBIDataset -WorkspaceId $wsId\n\nforeach ($ds in $datasets) {\n    $history = Invoke-PowerBIRestMethod \\\n      -Url \"groups/$wsId/datasets/$($ds.Id)/refreshes\" \\\n      -Method Get\n\n    $latest = $history.value[0]\n    if ($latest.status -eq 'Failed') {\n        Send-SlackAlert -Message @\"\n❌ Power BI Refresh Failed\nDataset: $($ds.Name)\nError: $($latest.serviceExceptionJson)\nTime: $($latest.endTime)\n\"@\n    }\n}") },
    { title: "report_design.txt", language: "Text", badge: "👤 User-Friendly", badgeColor: "#94e2d5", description: "Report design standards. At WebQ, we follow Power BI best practices for consistent, accessible reports.", lines: s("// WebQ Power BI Design Standards\n\n1. Page Layout\n   - 16:9 aspect ratio\n   - Header with logo + filters\n   - KPI cards at top\n   - Charts below, tables last\n\n2. Theme\n   - Use WebQ JSON theme file\n   - Consistent color palette\n   - Font: Segoe UI\n\n3. Navigation\n   - Page navigator buttons\n   - Drillthrough for details\n   - Back buttons on drillthrough pages\n   - Bookmarks for views\n\n4. Accessibility\n   - Alt text on all visuals\n   - Tab order configured\n   - High contrast theme available") },
    { title: "deployment_pipeline.yml", language: "YAML", badge: "📐 Best Practice", badgeColor: "#fab387", description: "Power BI deployment pipelines. At WebQ, we use deployment pipelines for dev/test/prod promotion.", lines: s("# Power BI Deployment Pipeline\n# Managed via Power BI Service\n\nStages:\n  Development:\n    workspace: WebQ-Analytics-Dev\n    refresh: manual\n    rls: test accounts\n\n  Test:\n    workspace: WebQ-Analytics-Test\n    refresh: daily\n    rls: mapped to test AD groups\n\n  Production:\n    workspace: WebQ-Analytics-Prod\n    refresh: scheduled (6 AM, 12 PM, 6 PM)\n    rls: mapped to production AD groups\n    gateway: WebQ-Enterprise-Gateway") },
    { title: "dataflow.json", language: "JSON", badge: "🔄 Event-Driven", badgeColor: "#74c7ec", description: "Dataflows for shared data prep. At WebQ, we centralize data preparation with reusable Power BI dataflows.", lines: s("{\n  \"name\": \"Orders Dataflow\",\n  \"entities\": [\n    {\n      \"name\": \"CleanedOrders\",\n      \"source\": \"Snowflake.analytics.orders\",\n      \"transformations\": [\n        {\"removeNulls\": [\"order_id\"]},\n        {\"filterRows\": \"status != 'cancelled'\"},\n        {\"addColumn\": {\n          \"name\": \"order_month\",\n          \"expression\": \"Date.Month([order_date])\"\n        }}\n      ],\n      \"refresh\": \"daily\",\n      \"loadEnabled\": true\n    }\n  ]\n}") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#f5c2e7", description: "Report documentation. At WebQ, we document every Power BI report with measure definitions and refresh schedules.", lines: s("# Sales Analytics Report\n\n## Measures\n| Measure | Formula | Format |\n|---------|---------|--------|\n| Revenue | SUM(fact_sales[Amount]) | $#,##0 |\n| YoY % | DIVIDE(Rev - PY_Rev, PY_Rev) | 0.0% |\n\n## Refresh Schedule\n- 6:00 AM, 12:00 PM, 6:00 PM UTC\n- Gateway: WebQ-Enterprise\n\n## RLS Roles\n- RegionalManager: Filter by region\n- DepartmentHead: Filter by org hierarchy\n\n## Data Sources\n- Snowflake via DirectQuery\n- Excel budget files via Import") },
  ],
  metrics: [
    { value: "#1", label: "BI Market Share", detail: "Most deployed" },
    { value: "300K+", label: "Organizations", detail: "Global customers" },
    { value: "Microsoft", label: "Ecosystem", detail: "365 integration" },
    { value: "DAX", label: "Formula Language", detail: "Powerful analytics" },
    { value: "Copilot", label: "AI Features", detail: "Natural language Q&A" },
    { value: "Free Tier", label: "Desktop App", detail: "No cost to start" },
    { value: "Embedded", label: "Analytics", detail: "API available" },
    { value: "Dataflows", label: "Data Prep", detail: "Self-service ETL" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 85, use: "Financial reporting, compliance" },
    { industry: "Healthcare", adoption: 70, use: "Clinical dashboards, operations" },
    { industry: "Retail", adoption: 80, use: "Sales performance, inventory" },
    { industry: "Manufacturing", adoption: 75, use: "Production KPIs, quality" },
    { industry: "Government", adoption: 72, use: "Budget tracking, performance" },
    { industry: "Education", adoption: 65, use: "Enrollment analytics, grants" },
  ],
  webqCallout: "At WebQ, we build Power BI solutions with optimized DAX, row-level security, and automated deployment pipelines — delivering enterprise analytics integrated with the Microsoft ecosystem.",
};

export const looker: TechSnippetData = {
  snippets: [
    { title: "access_grants.lkml", language: "LookML", badge: "🔒 Secure Code", badgeColor: "#f38ba8", description: "Access grants for field-level security. At WebQ, we use LookML access grants to control data visibility by role.", lines: s("access_grant: pii_access {\n  user_attribute: department\n  allowed_values: [\"hr\", \"compliance\", \"admin\"]\n}\n\nview: customers {\n  dimension: email {\n    type: string\n    sql: ${TABLE}.email ;;\n    required_access_grants: [pii_access]\n  }\n\n  dimension: customer_name {\n    type: string\n    sql: ${TABLE}.name ;;\n    required_access_grants: [pii_access]\n  }\n\n  dimension: customer_id {\n    type: number\n    sql: ${TABLE}.id ;;\n    # No access grant = visible to all\n  }\n}") },
    { title: "derived_table.lkml", language: "LookML", badge: "⚡ Performance", badgeColor: "#a6e3a1", description: "PDTs for pre-aggregated analytics. At WebQ, we use persistent derived tables with incremental builds for fast queries.", lines: s("view: daily_order_stats {\n  derived_table: {\n    sql:\n      SELECT\n        DATE(created_at) as order_date,\n        region,\n        COUNT(*) as order_count,\n        SUM(total) as revenue,\n        AVG(total) as aov\n      FROM orders\n      WHERE {% incrementally_where %}\n        created_at\n      {% endincrementally %}\n      GROUP BY 1, 2 ;;\n    datagroup_trigger: orders_datagroup\n    increment_key: order_date\n    increment_offset: 1\n  }\n}") },
    { title: "data_tests.lkml", language: "LookML", badge: "🧪 Testing", badgeColor: "#89b4fa", description: "LookML data tests for validation. At WebQ, we validate data integrity with automated LookML tests.", lines: s("test: orders_have_positive_total {\n  explore_source: orders {\n    column: count {}\n    filters: [orders.total: \"<0\"]\n  }\n  assert: {\n    expression: ${count} = 0 ;;\n    message: \"Found orders with negative totals\"\n  }\n}\n\ntest: no_orphaned_orders {\n  explore_source: orders {\n    column: count {}\n    filters: [customers.customer_id: \"NULL\"]\n  }\n  assert: {\n    expression: ${count} = 0 ;;\n    message: \"Orders without matching customer\"\n  }\n}") },
    { title: "system_activity.txt", language: "Text", badge: "📝 Logging", badgeColor: "#f9e2af", description: "System Activity dashboard for usage monitoring. At WebQ, we track query performance and user adoption.", lines: s("// Looker System Activity Queries\n\n// Top queries by runtime\nSELECT\n  query.model,\n  query.view,\n  history.average_runtime,\n  history.query_run_count\nFROM history\nJOIN query ON history.query_id = query.id\nWHERE history.created_date > '30 days ago'\nORDER BY history.average_runtime DESC\nLIMIT 20\n\n// User adoption tracking\nSELECT\n  user.name,\n  COUNT(*) as queries,\n  MAX(history.created_date) as last_active\nFROM history\nJOIN user ON history.user_id = user.id\nGROUP BY 1") },
    { title: "error_handling.lkml", language: "LookML", badge: "🛡️ Exception Handling", badgeColor: "#cba6f7", description: "Safe SQL with conditional logic. At WebQ, we handle null values and edge cases in LookML dimensions.", lines: s("view: orders {\n  measure: safe_average_total {\n    type: average\n    sql: CASE\n      WHEN ${total} > 0 AND ${total} < 1000000\n      THEN ${total}\n      ELSE NULL\n    END ;;\n    value_format_name: usd\n    description: \"Average order total, excluding outliers\"\n  }\n\n  dimension: order_status_label {\n    type: string\n    sql: COALESCE(\n      ${TABLE}.status,\n      'Unknown'\n    ) ;;\n    order_by_field: order_status_sort\n  }\n}") },
    { title: "explore.lkml", language: "LookML", badge: "👤 User-Friendly", badgeColor: "#94e2d5", description: "Curated Explores for self-service. At WebQ, we design Explores with field descriptions and logical grouping.", lines: s("explore: order_analysis {\n  label: \"Order Analytics\"\n  description: \"Analyze orders with customer and product details\"\n  group_label: \"Sales\"\n\n  always_filter: {\n    filters: [orders.created_date: \"30 days\"]\n  }\n\n  join: customers {\n    type: left_outer\n    relationship: many_to_one\n    sql_on: ${orders.customer_id} = ${customers.id} ;;\n    fields: [customers.name, customers.segment,\n             customers.lifetime_value]\n  }\n\n  query: default_revenue {\n    dimensions: [orders.created_month]\n    measures: [orders.total_revenue]\n  }\n}") },
    { title: "model.lkml", language: "LookML", badge: "📐 Best Practice", badgeColor: "#fab387", description: "Model structure with datagroups. At WebQ, we organize LookML with modular views, explores, and caching policies.", lines: s("connection: \"snowflake_analytics\"\n\ndatagroup: orders_datagroup {\n  sql_trigger: SELECT MAX(updated_at) FROM orders ;;\n  max_cache_age: \"24 hours\"\n}\n\npersist_with: orders_datagroup\n\n# Include views by directory\ninclude: \"/views/staging/*.view.lkml\"\ninclude: \"/views/marts/*.view.lkml\"\ninclude: \"/explores/*.explore.lkml\"\ninclude: \"/dashboards/*.dashboard.lkml\"\n\n# Map to dbt schema\nmap_layer: regions {\n  file: \"/maps/regions.topojson\"\n}") },
    { title: "action_hub.lkml", language: "LookML", badge: "🔄 Event-Driven", badgeColor: "#74c7ec", description: "Looker Actions for workflow triggers. At WebQ, we connect Looker to Slack, email, and webhooks for data-driven alerts.", lines: s("view: orders {\n  measure: total_revenue {\n    type: sum\n    sql: ${total} ;;\n    value_format_name: usd\n\n    # Slack alert when revenue drops\n    action: {\n      label: \"Alert Sales Team\"\n      url: \"https://hooks.slack.com/services/...\"\n      form_param: {\n        name: \"message\"\n        default: \"Revenue alert: {{ value }}\"\n      }\n    }\n  }\n}\n\n# Scheduled delivery\n# Dashboard delivered to Slack/email\n# at 8 AM daily with conditional alerts") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#f5c2e7", description: "LookML project documentation. At WebQ, we maintain comprehensive LookML docs with ERD and field dictionary.", lines: s("# Analytics LookML Project\n\n## Structure\n```\nviews/\n  staging/    (1:1 with dbt staging)\n  marts/      (business-ready views)\nexplores/     (curated data access)\ndashboards/   (official dashboards)\n```\n\n## Key Explores\n| Explore | Description |\n|---------|-------------|\n| order_analysis | Sales analytics |\n| customer_360 | Customer insights |\n\n## Datagroups\n- orders: Triggered by MAX(updated_at)\n- daily: Max cache 24 hours") },
  ],
  metrics: [
    { value: "Google Cloud", label: "Platform", detail: "GCP native" },
    { value: "LookML", label: "Semantic Layer", detail: "Git-versioned" },
    { value: "2,500+", label: "Customers", detail: "Enterprise adoption" },
    { value: "API-First", label: "Architecture", detail: "Embedded analytics" },
    { value: "Governed", label: "Self-Service", detail: "Curated explores" },
    { value: "Git", label: "Version Control", detail: "LookML in Git" },
    { value: "100+", label: "Database Dialects", detail: "Universal SQL" },
    { value: "Actions", label: "Workflow", detail: "Data-driven triggers" },
  ],
  industries: [
    { industry: "Technology", adoption: 80, use: "Product analytics, user metrics" },
    { industry: "E-commerce", adoption: 72, use: "Revenue analytics, funnel analysis" },
    { industry: "SaaS", adoption: 78, use: "ARR tracking, churn analysis" },
    { industry: "Healthcare", adoption: 55, use: "Clinical analytics" },
    { industry: "Financial Services", adoption: 60, use: "Portfolio analytics" },
    { industry: "Media", adoption: 65, use: "Content engagement, ad analytics" },
  ],
  webqCallout: "At WebQ, we build Looker projects with modular LookML, curated Explores, and embedded analytics — delivering governed, self-service BI powered by a semantic modeling layer.",
};

export const databricks: TechSnippetData = {
  snippets: [
    { title: "unity_catalog.sql", language: "SQL", badge: "🔒 Secure Code", badgeColor: "#f38ba8", description: "Unity Catalog for data governance. At WebQ, we enforce fine-grained access control with Unity Catalog's three-level namespace.", lines: s("-- Unity Catalog data governance\nCREATE CATALOG analytics;\nCREATE SCHEMA analytics.orders;\n\n-- Grant access\nGRANT USE CATALOG ON CATALOG analytics\n  TO `data-engineers`;\nGRANT USE SCHEMA ON SCHEMA analytics.orders\n  TO `data-engineers`;\nGRANT SELECT ON TABLE analytics.orders.fact_orders\n  TO `analysts`;\n\n-- Column masking\nCREATE FUNCTION mask_email(email STRING)\n  RETURNS STRING\n  RETURN CONCAT('***@', SPLIT(email, '@')[1]);\n\nALTER TABLE customers ALTER COLUMN email\n  SET MASK mask_email;") },
    { title: "photon_optimization.py", language: "Python", badge: "⚡ Performance", badgeColor: "#a6e3a1", description: "Photon engine with Delta optimization. At WebQ, we optimize Databricks with Z-ordering, auto-compaction, and liquid clustering.", lines: s("# Delta Lake optimizations\nspark.sql(\"\"\"\n  OPTIMIZE analytics.fact_orders\n  ZORDER BY (order_date, region)\n\"\"\")\n\n# Auto-optimization settings\nspark.sql(\"\"\"\n  ALTER TABLE analytics.fact_orders SET TBLPROPERTIES (\n    'delta.autoOptimize.optimizeWrite' = 'true',\n    'delta.autoOptimize.autoCompact' = 'true',\n    'delta.tuneFileSizesForRewrites' = 'true'\n  )\n\"\"\")\n\n# Liquid clustering (Databricks 13.3+)\nspark.sql(\"\"\"\n  ALTER TABLE analytics.fact_orders\n  CLUSTER BY (region, order_date)\n\"\"\")") },
    { title: "test_notebook.py", language: "Python", badge: "🧪 Testing", badgeColor: "#89b4fa", description: "Data quality testing with expectations. At WebQ, we validate every pipeline stage with Delta Live Tables expectations.", lines: s("import dlt\nfrom pyspark.sql.functions import col\n\n@dlt.table(\n  comment='Validated orders'\n)\n@dlt.expect_or_drop('valid_total', 'total > 0')\n@dlt.expect_or_fail('valid_id', 'order_id IS NOT NULL')\n@dlt.expect('valid_date', 'order_date <= current_date()')\ndef cleaned_orders():\n    return (\n        dlt.read('raw_orders')\n        .filter(col('status') != 'test')\n        .dropDuplicates(['order_id'])\n    )\n\n# Pipeline automatically tracks\n# - Records meeting expectations\n# - Records dropped or failed\n# - Data quality metrics over time") },
    { title: "mlflow_tracking.py", language: "Python", badge: "📝 Logging", badgeColor: "#f9e2af", description: "MLflow experiment tracking on Databricks. At WebQ, we track every ML experiment with auto-logging and model registry.", lines: s("import mlflow\nmlflow.autolog()\n\nwith mlflow.start_run(run_name='churn-xgb-v3'):\n    model = xgb.XGBClassifier(n_estimators=200)\n    model.fit(X_train, y_train)\n\n    predictions = model.predict(X_test)\n    f1 = f1_score(y_test, predictions)\n\n    # Auto-logged: params, metrics, model\n    mlflow.log_metric('custom_f1', f1)\n\n    # Register model\n    mlflow.register_model(\n        f'runs:/{mlflow.active_run().info.run_id}/model',\n        'churn-classifier'\n    )") },
    { title: "error_handling.py", language: "Python", badge: "🛡️ Exception Handling", badgeColor: "#cba6f7", description: "Structured Streaming error recovery. At WebQ, we implement checkpointing and dead-letter patterns for streaming jobs.", lines: s("from pyspark.sql.functions import col, current_timestamp\n\ndef process_stream_with_recovery():\n    try:\n        stream = spark.readStream \\\n            .format('delta') \\\n            .table('raw.events')\n\n        valid = stream.filter(col('event_id').isNotNull())\n        invalid = stream.filter(col('event_id').isNull())\n\n        # Good records\n        valid.writeStream \\\n            .option('checkpointLocation', '/checkpoints/events') \\\n            .table('analytics.events') \\\n            .start()\n\n        # Dead letter\n        invalid.writeStream \\\n            .option('checkpointLocation', '/checkpoints/dlq') \\\n            .table('analytics.events_dlq') \\\n            .start()\n    except Exception as e:\n        log_error(e)") },
    { title: "notebook_widgets.py", language: "Python", badge: "👤 User-Friendly", badgeColor: "#94e2d5", description: "Interactive notebook widgets. At WebQ, we build parameterized notebooks for analyst self-service.", lines: s("# Interactive widgets for analysts\ndbutils.widgets.dropdown('region', 'all',\n    ['all', 'us-east', 'us-west', 'eu', 'apac'])\ndbutils.widgets.text('start_date', '2024-01-01')\ndbutils.widgets.text('min_revenue', '100')\n\nregion = dbutils.widgets.get('region')\nstart_date = dbutils.widgets.get('start_date')\n\nquery = f\"\"\"\n    SELECT * FROM analytics.fact_orders\n    WHERE order_date >= '{start_date}'\n    {'AND region = \"' + region + '\"' if region != 'all' else ''}\n    AND total >= {dbutils.widgets.get('min_revenue')}\n\"\"\"\ndf = spark.sql(query)\ndisplay(df)") },
    { title: "dlt_pipeline.py", language: "Python", badge: "📐 Best Practice", badgeColor: "#fab387", description: "Delta Live Tables for declarative pipelines. At WebQ, we use DLT for medallion architecture with automatic dependency management.", lines: s("import dlt\n\n# Bronze: raw ingestion\n@dlt.table(comment='Raw order events')\ndef bronze_orders():\n    return spark.readStream.format('cloudFiles') \\\n        .option('cloudFiles.format', 'json') \\\n        .load('/data/orders/')\n\n# Silver: cleaned and validated\n@dlt.table(comment='Cleaned orders')\n@dlt.expect_all_or_drop({\n    'valid_id': 'order_id IS NOT NULL',\n    'valid_total': 'total > 0'\n})\ndef silver_orders():\n    return dlt.read_stream('bronze_orders') \\\n        .select('order_id', 'customer_id', 'total', 'order_date')\n\n# Gold: aggregated\n@dlt.table(comment='Daily revenue')\ndef gold_daily_revenue():\n    return dlt.read('silver_orders') \\\n        .groupBy('order_date').agg(sum('total').alias('revenue'))") },
    { title: "workflows.json", language: "JSON", badge: "🔄 Event-Driven", badgeColor: "#74c7ec", description: "Databricks Workflows for orchestration. At WebQ, we schedule and chain notebook tasks with retry logic.", lines: s("{\n  \"name\": \"Daily Analytics Pipeline\",\n  \"schedule\": {\n    \"quartz_cron_expression\": \"0 0 6 * * ?\",\n    \"timezone_id\": \"UTC\"\n  },\n  \"tasks\": [\n    {\n      \"task_key\": \"ingest\",\n      \"notebook_task\": {\n        \"notebook_path\": \"/pipelines/ingest\"\n      },\n      \"max_retries\": 2\n    },\n    {\n      \"task_key\": \"transform\",\n      \"depends_on\": [{\"task_key\": \"ingest\"}],\n      \"notebook_task\": {\n        \"notebook_path\": \"/pipelines/transform\"\n      }\n    }\n  ]\n}") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#f5c2e7", description: "Databricks project documentation. At WebQ, we document lakehouse architecture, catalog structure, and pipeline dependencies.", lines: s("# Analytics Lakehouse\n\n## Architecture (Medallion)\n```\nBronze → Silver → Gold → BI\n```\n\n## Catalog Structure\n| Catalog | Schema | Purpose |\n|---------|--------|--------|\n| raw | events | Raw ingestion |\n| analytics | orders | Cleaned data |\n| analytics | marts | Aggregated |\n\n## Pipelines\n- DLT: orders_pipeline (continuous)\n- Workflow: daily_analytics (6 AM UTC)\n\n## Compute\n- Jobs cluster: 4-16 workers (autoscale)\n- SQL Warehouse: Medium (serverless)") },
  ],
  metrics: [
    { value: "$1.6B+", label: "Annual Revenue", detail: "Market leader" },
    { value: "10K+", label: "Customers", detail: "Enterprise adoption" },
    { value: "#1", label: "Lakehouse Platform", detail: "Industry pioneer" },
    { value: "Delta Lake", label: "Open Format", detail: "ACID on data lakes" },
    { value: "Unity Catalog", label: "Governance", detail: "Fine-grained ACL" },
    { value: "Photon", label: "Engine", detail: "2x Spark performance" },
    { value: "MLflow", label: "Built-in MLOps", detail: "Experiment tracking" },
    { value: "Multi-Cloud", label: "Availability", detail: "AWS, Azure, GCP" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 80, use: "Risk analytics, regulatory reporting" },
    { industry: "Healthcare", adoption: 70, use: "Genomics, clinical analytics" },
    { industry: "Retail", adoption: 75, use: "Customer 360, demand forecasting" },
    { industry: "Media", adoption: 68, use: "Content recommendations, ad analytics" },
    { industry: "Technology", adoption: 85, use: "Product analytics, ML platform" },
    { industry: "Manufacturing", adoption: 60, use: "IoT analytics, quality" },
  ],
  webqCallout: "At WebQ, we architect Databricks lakehouses with Delta Live Tables, Unity Catalog governance, and MLflow — delivering unified analytics and ML at enterprise scale.",
};

export const apacheFlink: TechSnippetData = {
  snippets: [
    { title: "security_config.java", language: "Java", badge: "🔒 Secure Code", badgeColor: "#f38ba8", description: "SSL and Kerberos authentication. At WebQ, we secure Flink clusters with encrypted communication and RBAC.", lines: s("// Flink security configuration\nConfiguration config = new Configuration();\nconfig.set(SecurityOptions.SSL_INTERNAL_ENABLED, true);\nconfig.set(SecurityOptions.SSL_REST_ENABLED, true);\nconfig.set(SecurityOptions.SSL_KEYSTORE,\n    \"/certs/flink.keystore.jks\");\nconfig.set(SecurityOptions.SSL_TRUSTSTORE,\n    \"/certs/flink.truststore.jks\");\n\n// Kerberos authentication\nconfig.set(SecurityOptions.KERBEROS_LOGIN_KEYTAB,\n    \"/keytabs/flink.keytab\");\nconfig.set(SecurityOptions.KERBEROS_LOGIN_PRINCIPAL,\n    \"flink/host@WEBQ.COM\");") },
    { title: "optimized_pipeline.java", language: "Java", badge: "⚡ Performance", badgeColor: "#a6e3a1", description: "Optimized stream processing with state backends. At WebQ, we tune Flink with RocksDB state and checkpointing for high throughput.", lines: s("StreamExecutionEnvironment env =\n    StreamExecutionEnvironment.getExecutionEnvironment();\n\n// RocksDB state backend for large state\nenv.setStateBackend(new EmbeddedRocksDBStateBackend());\nenv.getCheckpointConfig().setCheckpointStorage(\n    \"s3://checkpoints/flink/\");\n\n// Checkpoint tuning\nenv.enableCheckpointing(60000); // 60s\nenv.getCheckpointConfig().setMinPauseBetweenCheckpoints(30000);\nenv.getCheckpointConfig().setTolerableCheckpointFailureNumber(3);\n\n// Watermark strategy for event time\nDataStream<Event> events = source\n    .assignTimestampsAndWatermarks(\n        WatermarkStrategy.<Event>forBoundedOutOfOrderness(\n            Duration.ofSeconds(10)));") },
    { title: "pipeline_test.java", language: "Java", badge: "🧪 Testing", badgeColor: "#89b4fa", description: "MiniCluster testing for Flink jobs. At WebQ, we test stream processing logic with harnesses and test sources.", lines: s("@ExtendWith(MiniClusterExtension.class)\nclass FraudDetectionTest {\n    @Test\n    void detectsFraudulentTransaction() throws Exception {\n        StreamExecutionEnvironment env =\n            StreamExecutionEnvironment.getExecutionEnvironment();\n\n        List<Transaction> input = Arrays.asList(\n            new Transaction(\"user1\", 5000.0, \"card\"),\n            new Transaction(\"user1\", 4999.0, \"card\")\n        );\n\n        DataStream<Alert> alerts = env\n            .fromCollection(input)\n            .keyBy(Transaction::getUserId)\n            .process(new FraudDetector());\n\n        List<Alert> results = alerts.executeAndCollect(10);\n        assertEquals(1, results.size());\n    }\n}") },
    { title: "metrics_reporter.java", language: "Java", badge: "📝 Logging", badgeColor: "#f9e2af", description: "Custom metrics with Prometheus reporter. At WebQ, we monitor Flink job health with custom counters and gauges.", lines: s("// Custom metric in process function\npublic class FraudDetector\n    extends KeyedProcessFunction<String, Txn, Alert> {\n\n    private transient Counter fraudCounter;\n    private transient Histogram latencyHist;\n\n    @Override\n    public void open(Configuration params) {\n        fraudCounter = getRuntimeContext()\n            .getMetricGroup()\n            .counter(\"fraud_detected\");\n        latencyHist = getRuntimeContext()\n            .getMetricGroup()\n            .histogram(\"processing_latency\",\n                new DescriptiveStatisticsHistogram(1000));\n    }\n\n    @Override\n    public void processElement(Txn txn, Context ctx, Collector<Alert> out) {\n        long start = System.currentTimeMillis();\n        // ... processing logic\n        latencyHist.update(System.currentTimeMillis() - start);\n    }\n}") },
    { title: "error_handling.java", language: "Java", badge: "🛡️ Exception Handling", badgeColor: "#cba6f7", description: "Side outputs for error routing. At WebQ, we route malformed events to dead-letter streams without stopping the pipeline.", lines: s("final OutputTag<String> deadLetterTag =\n    new OutputTag<>(\"dead-letter\") {};\n\nSingleOutputStreamOperator<Order> processed = events\n    .process(new ProcessFunction<String, Order>() {\n        @Override\n        public void processElement(String json, Context ctx,\n                Collector<Order> out) {\n            try {\n                Order order = parse(json);\n                validate(order);\n                out.collect(order);\n            } catch (Exception e) {\n                ctx.output(deadLetterTag,\n                    json + \"|ERROR:\" + e.getMessage());\n            }\n        }\n    });\n\nprocessed.getSideOutput(deadLetterTag)\n    .sinkTo(kafkaSink(\"dead-letter-topic\"));") },
    { title: "flink_sql.sql", language: "SQL", badge: "👤 User-Friendly", badgeColor: "#94e2d5", description: "Flink SQL for stream processing. At WebQ, we use Flink SQL for analyst-friendly real-time analytics.", lines: s("-- Flink SQL for real-time analytics\nCREATE TABLE orders (\n    order_id STRING,\n    customer_id STRING,\n    total DECIMAL(10,2),\n    order_time TIMESTAMP(3),\n    WATERMARK FOR order_time AS order_time - INTERVAL '10' SECOND\n) WITH (\n    'connector' = 'kafka',\n    'topic' = 'orders',\n    'format' = 'json'\n);\n\n-- Tumbling window aggregation\nSELECT\n    TUMBLE_START(order_time, INTERVAL '5' MINUTE) AS window_start,\n    COUNT(*) AS order_count,\n    SUM(total) AS revenue\nFROM orders\nGROUP BY TUMBLE(order_time, INTERVAL '5' MINUTE);") },
    { title: "savepoint.sh", language: "Bash", badge: "📐 Best Practice", badgeColor: "#fab387", description: "Savepoint management for upgrades. At WebQ, we use savepoints for zero-downtime Flink job upgrades.", lines: s("#!/bin/bash\n# Graceful job upgrade with savepoint\n\n# 1. Trigger savepoint\nflink savepoint $JOB_ID s3://savepoints/\nSAVEPOINT_PATH=$(flink savepoint $JOB_ID s3://savepoints/ | grep 'Savepoint completed')\n\n# 2. Cancel old job\nflink cancel $JOB_ID\n\n# 3. Start new version from savepoint\nflink run \\\n  -s $SAVEPOINT_PATH \\\n  -d fraud-detector-v2.jar\n\n# 4. Verify new job is processing\nflink list --running\n\necho 'Upgrade complete with zero data loss'") },
    { title: "cep_pattern.java", language: "Java", badge: "🔄 Event-Driven", badgeColor: "#74c7ec", description: "Complex Event Processing with Flink CEP. At WebQ, we detect multi-event patterns for fraud and anomaly detection.", lines: s("// Detect rapid successive transactions\nPattern<Transaction, ?> fraudPattern = Pattern\n    .<Transaction>begin(\"first\")\n    .where(new SimpleCondition<Transaction>() {\n        public boolean filter(Transaction t) {\n            return t.getAmount() > 1000;\n        }\n    })\n    .next(\"second\")\n    .where(new SimpleCondition<Transaction>() {\n        public boolean filter(Transaction t) {\n            return t.getAmount() > 1000;\n        }\n    })\n    .within(Time.minutes(5));\n\nPatternStream<Transaction> patternStream =\n    CEP.pattern(txnStream.keyBy(Transaction::getUserId),\n        fraudPattern);") },
    { title: "README.md", language: "Markdown", badge: "📖 Documentation", badgeColor: "#f5c2e7", description: "Flink job documentation. At WebQ, we document every streaming job with topology, state, and SLA details.", lines: s("# Fraud Detection Pipeline\n\n## Topology\nKafka → Parse → Validate → Detect → Alert\n\n## State\n- Backend: RocksDB (incremental checkpoints)\n- Checkpoint: Every 60s to S3\n- Savepoints: Before every upgrade\n\n## SLA\n- Latency: < 500ms (p99)\n- Throughput: 100K events/sec\n- Availability: 99.9%\n\n## Scaling\n- Parallelism: 16 (4 TaskManagers × 4 slots)\n- State size: ~50GB (RocksDB)\n\n## Alerts\n- PagerDuty: stream-oncall\n- Grafana: flink-dashboard") },
  ],
  metrics: [
    { value: "24K+", label: "GitHub Stars", detail: "Apache Flink repo" },
    { value: "#1", label: "Stream Processing", detail: "Low latency standard" },
    { value: "Exactly-Once", label: "Semantics", detail: "Guaranteed delivery" },
    { value: "Sub-Second", label: "Latency", detail: "Event processing" },
    { value: "PB-Scale", label: "Throughput", detail: "Alibaba scale" },
    { value: "Confluent", label: "Cloud Offering", detail: "Managed Flink" },
    { value: "SQL", label: "Support", detail: "Analyst-friendly" },
    { value: "CEP", label: "Built-in", detail: "Pattern detection" },
  ],
  industries: [
    { industry: "Financial Services", adoption: 78, use: "Real-time fraud detection" },
    { industry: "E-commerce", adoption: 72, use: "Real-time recommendations, pricing" },
    { industry: "Telecommunications", adoption: 70, use: "Network event processing" },
    { industry: "Ad Tech", adoption: 75, use: "Real-time bidding, attribution" },
    { industry: "IoT / Manufacturing", adoption: 65, use: "Sensor data processing" },
    { industry: "Gaming", adoption: 58, use: "Real-time leaderboards, analytics" },
  ],
  webqCallout: "At WebQ, we build Apache Flink pipelines for real-time event processing — with exactly-once semantics, CEP for pattern detection, and zero-downtime upgrades via savepoints.",
};
